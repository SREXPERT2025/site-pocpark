# РОСПАРК — публичный SEO/GEO baseline

Дата исходной проверки: 2026-07-23
Дата контрольного среза: 2026-08-04
Связанный roadmap: `docs/site/SITE_DEVELOPMENT_ROADMAP_20260723.md`
Контур: публичный production и локальный код, только read-only
Статус: публичная техническая часть завершена; права в Google и Яндексе
подтверждены; Search Console и Яндекс Вебмастер накопили первый измеримый
поисковый срез; начат первый пакет исправлений по фактическим данным кабинетов.

## 1. Назначение и границы

Документ фиксирует фактическую исходную точку после Demo Release v1:

- доступность индексируемых URL;
- `robots.txt` и sitemap;
- canonical, metadata, H1 и JSON-LD;
- границы индексации demo и quiz;
- наблюдаемую публичную поисковую выдачу;
- пакет данных, который нужно получить из Google Search Console и Яндекс
  Вебмастера.

Публичная выдача не показывает точное число проиндексированных URL и не
заменяет данные поисковых кабинетов. В ходе проверки не изменялись код,
production, Nginx, DNS, sitemap, metadata и настройки поисковых систем.

## 2. Краткий вывод

Техническая SEO-основа сайта в рабочем состоянии:

- `robots.txt` разрешает обход и ссылается на актуальный sitemap;
- sitemap содержит 75 уникальных URL на едином `www` origin;
- все 75 URL отвечают `200`;
- среди URL sitemap нет `noindex`;
- у всех 75 страниц есть `title`, `description`, один H1 и JSON-LD;
- внутренние demo-сценарии и `/quiz` исключены из sitemap и закрыты от
  индексации;
- `/demo` остаётся индексируемым коммерческим хабом.

Обнаружены два ограниченных технических расхождения:

1. HTTPS-версия без `www` отвечает `200`, а не перенаправляет на `www`.
   Canonical указывает на `www`, но постоянный redirect был бы сильнее и
   устранил бы доступность дубля.
2. Страницы `/privacy` и
   `/soglasie-na-obrabotku-personalnyh-dannyh` наследуют metadata главной
   страницы, не имеют собственного canonical и образуют группу одинаковых
   `title`.

Главный оставшийся пробел — не техническая доступность сайта, а отсутствие
подтверждённых и накопленных данных Search Console и Яндекс Вебмастера.
Resources созданы, права подтверждены. Проверенный sitemap отправлен в оба
кабинета 2026-07-23. Контроль 2026-07-24 показал:

- Google обработал sitemap со статусом «Успешно» и обнаружил 75 страниц;
- отчёт «Индексирование страниц» Google ещё формируется;
- Яндекс сохраняет sitemap в очереди на обработку со сроком до 1–2 недель;
- раздел «Страницы в поиске» Яндекса пока показывает 0 добавленных, 0
  удалённых и отсутствие свежих изменений.

До обработки отчётов нельзя достоверно утверждать, сколько страниц уже
проиндексировано, какие запросы получают показы и почему поисковик исключает
отдельные URL.

## 3. Проверенный публичный контур

### 3.1. Host и redirect

| Запрос | Результат | Оценка |
|---|---|---|
| `http://роспарк.рф/` | `301` на HTTPS без `www` | HTTPS включён |
| `https://роспарк.рф/` | `200` | доступен дубль origin |
| `http://www.роспарк.рф/` | `301` на HTTPS `www` | HTTPS включён |
| `https://www.роспарк.рф/` | `200` | основной origin |

Обе HTTPS-версии показывают текущий сайт. Canonical главной страницы у обеих
версий ведёт на:

```text
https://www.xn--80aukedde.xn--p1ai
```

Задача `SEO-CANONICAL-001`: после отдельного согласования настроить на Nginx
постоянный redirect с HTTPS non-`www` на соответствующий HTTPS `www` URL,
сохраняя path и query string. Перед изменением нужны backup конфигурации,
`nginx -t`, smoke и rollback. Эта задача не требует менять приложение.

### 3.2. robots.txt

Публичный файл:

```text
User-Agent: *
Allow: /
Sitemap: https://www.xn--80aukedde.xn--p1ai/sitemap.xml
```

Оценка: корректно для текущей открытой индексации. Чувствительные и
непредназначенные для поиска маршруты закрываются через metadata `noindex`, а
не только через robots.

### 3.3. Sitemap

Фактический результат:

```text
URL всего:                 75
уникальных URL:            75
URL на www origin:         75
URL с lastmod:             56
HTTP 200:                  75
не-200:                     0
```

Отсутствие `lastmod` у части статических страниц само по себе не считается
ошибкой. Для страниц с `lastmod` остаётся отдельная задача: не использовать
filesystem mtime как редакционную дату и хранить подтверждённые даты публикации
и обновления контента.

### 3.4. Полный HTML-аудит URL из sitemap

| Проверка | Результат |
|---|---:|
| Ошибки загрузки | 0 |
| Не-200 ответы | 0 |
| `noindex` внутри sitemap | 0 |
| Страницы без `title` | 0 |
| Страницы без `description` | 0 |
| Страницы с количеством H1 не равным одному | 0 |
| Страницы без JSON-LD | 0 |
| Страницы без canonical | 2 |

Canonical отсутствует у:

```text
/privacy
/soglasie-na-obrabotku-personalnyh-dannyh
```

Эти же страницы наследуют title главной:

```text
РОСПАРК — автоматизация парковок нового поколения
```

Локальная причина подтверждена: обе страницы не экспортируют собственный
`metadata` и наследуют root metadata из `app/layout.tsx`.

Задача `SEO-META-LEGAL-001`: добавить обеим юридическим страницам уникальные
`title`, `description` и self-canonical.

Production-статус 2026-07-24:

- исправление реализовано в ветке `fix/site-20260723-legal-metadata`;
- обе страницы экспортируют собственный `Metadata`;
- canonical формируется единым helper `canonicalUrl(...)`;
- юридический текст и sitemap не изменялись;
- typecheck, lint, production build и проверка сгенерированного HTML пройдены;
- исправление опубликовано в release
  `9ae9579c63dc8c3c7af96a1e46d87ee0081b56da`.

## 4. Граница индексации demo и служебных страниц

| URL | HTTP | Robots metadata | Sitemap | Оценка |
|---|---:|---|---|---|
| `/demo` | `200` | индексируем | включён | правильно |
| `/demo/gostevaya-zayavka` | `200` | `noindex, nofollow, nocache` | исключён | правильно |
| `/demo/web-skidki` | `200` | `noindex, nofollow, nocache` | исключён | правильно |
| `/demo/vladelec-parkovki` | `200` | `noindex, nofollow, nocache` | исключён | правильно |
| `/demo/arendar/demo-01` | `200` | `noindex, nofollow, nocache` | исключён | правильно |
| `/quiz` | `200` | `noindex, follow` | исключён | правильно |

Не нужно добавлять внутренние demo-кабинеты в sitemap. Отсутствие canonical у
части `noindex` demo-страниц не является текущим приоритетом: важнее сохранить
их закрытыми и не создавать индексируемые token/user-specific URL.

## 5. Публичная поисковая видимость

Проверены выборочные запросы:

```text
site:роспарк.рф
site:xn--80aukedde.xn--p1ai
"РОСПАРК" автоматизация парковок
"роспарк.рф"
```

В публичной выдаче обнаруживаются:

- новая главная на `www`;
- `/resheniya` и несколько новых страниц ролей/решений;
- отдельные страницы кейсов и оборудования;
- старый результат корня без `www` с устаревшим сниппетом;
- внешний результат РОСПАРК на Rutube.

Выборочные узкие `site:`-запросы по `/demo`, `/stati`, `/keysy` и
`/oborudovanie` не дали стабильного набора результатов в использованном
публичном поисковом proxy. Это не доказывает отсутствие страниц в индексе:
операторы `site:` дают неполную и меняющуюся выборку.

Практический вывод:

- поисковые системы уже обнаруживают новый сайт;
- признаки старого non-`www` результата сохраняются;
- точный индекс, выбранные canonical и поисковый спрос необходимо получить из
  кабинетов.

## 6. Пакет Google Search Console

Нужен владелец или пользователь с доступом к property сайта. Никакие изменения
в кабинете не выполнять до фиксации исходного состояния.

### 6.1. Текущее состояние аккаунта

Read-only проверка 2026-07-23 показала:

- пользователь авторизован;
- создана Domain property `роспарк.рф`;
- Google verification TXT-запись добавлена на корневой домен;
- запись подтверждена на авторитетных DNS REG.RU, системном resolver, Google
  Public DNS и Cloudflare DNS;
- право собственности успешно подтверждено в Search Console;
- обзор, эффективность, индексирование и качество показывают «Данные
  обрабатываются, повторите попытку приблизительно через день»;
- `https://www.роспарк.рф/sitemap.xml` отправлен 2026-07-23;
- интерфейс подтвердил «Файл Sitemap отправлен»;
- первоначальный статус строки — «Не получено», выявлено 0 страниц и 0 видео;
- контроль 2026-07-24: статус — «Успешно», дата последней обработки —
  2026-07-23, выявлено 75 страниц и 0 видео;
- отчёт «Индексирование страниц» 2026-07-24 всё ещё показывает «Данные
  обрабатываются, повторите попытку приблизительно через день»;
- в коде приложения не обнаружен `google-site-verification`.

DNS домена обслуживается:

```text
ns1.reg.ru
ns2.reg.ru
```

Выбранный вариант — Domain property для `xn--80aukedde.xn--p1ai` с
подтверждением отдельной TXT-записью в DNS. Domain property объединит данные
HTTP/HTTPS, `www` и non-`www`. Не удалять TXT после успешного подтверждения:
поисковая система может периодически повторно проверять права.

### 6.2. Проверить property

- domain property для `роспарк.рф`, если доступна;
- URL-prefix `https://www.роспарк.рф/`;
- URL-prefix `https://роспарк.рф/`, если она заведена отдельно;
- подтверждённого владельца и способ верификации;
- отправлен ли `sitemap.xml`, дата чтения и результат обработки.

### 6.3. Снять исходные данные

Экспортировать или сделать скриншоты без персональных данных:

1. Page indexing:
   - indexed;
   - not indexed;
   - причины исключения и количество URL;
   - последний crawl/обновление отчёта.
2. Sitemaps:
   - submitted URL;
   - status;
   - discovered pages;
   - last read.
3. Search results за 7, 28 и 90 дней:
   - clicks;
   - impressions;
   - CTR;
   - average position;
   - queries;
   - pages;
   - countries;
   - devices.
4. Core Web Vitals и HTTPS.
5. Crawl stats, если отчёт доступен.
6. Manual actions и Security issues — только наличие/отсутствие, без
   исправлений в рамках этого блока.

### 6.4. URL Inspection

Проверить минимум:

```text
/
/resheniya
/demo
/stati
/keysy
/oborudovanie
/privacy
/soglasie-na-obrabotku-personalnyh-dannyh
```

Для каждой страницы записать:

```text
URL
URL is on Google / is not on Google
last crawl
crawl allowed
indexing allowed
user-declared canonical
Google-selected canonical
referring sitemap
page fetch result
```

Отдельно проверить non-`www` корень и сравнить Google-selected canonical с
`www`.

## 7. Пакет Яндекс Вебмастера

### 7.1. Текущее состояние аккаунта

Read-only проверка 2026-07-23 показала:

- пользователь авторизован;
- создан сайт `https://www.роспарк.рф/`;
- Yandex verification TXT-запись первоначально добавлена на корневой домен;
- Яндекс вернул «Нет записи с ключом для подтверждения», поскольку проверяется
  конкретный `www`-host;
- отдельная TXT с тем же Yandex verification value добавлена на subdomain
  `www` и видна через `ns1.reg.ru`, `ns2.reg.ru`, системный resolver, Google
  Public DNS и Cloudflare DNS;
- право успешно подтверждено, аккаунт получил роль «Владелец»;
- в сводке нет текущих ошибок и рекомендаций;
- массовые дубли title/description не обнаружены;
- на контрольном срезе 2026-07-24 новая статистика поиска и индексирования ещё
  не сформирована;
- раздел «Страницы в поиске» показывает 0 добавленных, 0 удалённых и отсутствие
  новых изменений;
- `https://www.роспарк.рф/sitemap.xml` добавлен 2026-07-23;
- статус на 2026-07-24 — «Очередь на обработку»; интерфейс указывает срок до
  1–2 недель;
- сводка сохраняет старую историю обхода 2024 года с legacy URL; её не считать
  состоянием нового сайта до свежего обхода;
- в коде приложения не обнаружен `yandex-verification`.

Предпочтительный стартовый URL:

```text
https://www.xn--80aukedde.xn--p1ai/
```

Яндекс считает `www` и non-`www` разными адресами. Для выбранного сайта
опубликована TXT на `www`; корневая запись оставлена. После подтверждения прав
отдельно проверить выбранное главное зеркало. Не удалять TXT после успешного
подтверждения: Яндекс периодически повторно проверяет права.

### 7.2. Проверить сайт и зеркало

- подтверждены ли права на HTTPS `www` и доступные варианты host;
- какой host Яндекс считает главным зеркалом;
- присутствует ли `sitemap.xml`, его статус и дата загрузки;
- корректны ли региональность и организация, если эти данные заведены;
- нет ли критических сообщений диагностики.

### 7.3. Снять исходные данные

Экспортировать или сделать скриншоты:

1. Индексирование → Страницы в поиске:
   - в поиске;
   - исключено;
   - причины исключения.
2. Индексирование → Статистика обхода:
   - загруженные URL;
   - коды ответов;
   - дата последнего обхода.
3. Индексирование → Файлы Sitemap:
   - URL;
   - статус;
   - количество найденных страниц;
   - ошибки.
4. Поисковые запросы за доступные 7, 30 и 90 дней:
   - показы;
   - клики;
   - CTR;
   - позиции;
   - запросы;
   - страницы входа.
5. Диагностика сайта и нарушения.
6. Качество сайта и внешние ссылки — как справочный baseline, без
   искусственного наращивания ссылок.

### 7.4. Проверка URL

Использовать тот же набор URL, что для Search Console, и зафиксировать:

```text
присутствие в поиске
последний обход
HTTP status
robots
canonical
выбранное зеркало
причина исключения
```

## 8. Формат сводного evidence

Результаты кабинетов добавить отдельным датированным приложением:

```text
источник: Google / Яндекс
property или host
период
дата выгрузки
indexed / excluded
причины исключений
sitemap status
брендовые запросы
небрендовые запросы
страницы входа
позиция / показы / клики / CTR
проверенные URL и выбранный canonical
вывод
следующее действие
ответственный
```

Не сохранять в git cookies, verification token, recovery code, логины,
персональные данные и полные приватные выгрузки. В репозиторий помещать только
агрегированную сводку без секретов.

## 9. Приоритет дальнейших действий

1. `SEO-SITEMAP-SUBMIT-001` — выполнен 2026-07-23: sitemap отправлен в оба
   кабинета, начальный статус зафиксирован.
2. `SEO-OPS-001-B` — выполнен 2026-08-04: получены эффективность,
   индексирование, sitemap и Core Web Vitals из Search Console, а также
   диагностика, поисковые запросы и история обхода из Яндекс Вебмастера.
3. `SEO-DESCRIPTION-001` — устранить подтверждённый Яндексом дубль description
   у `/oborudovanie/shlagbaum-rospark-3` и
   `/oborudovanie/shlagbaum-rospark-4`, затем дождаться переобхода.
4. `SEO-META-LEGAL-001` — выполнен и опубликован 2026-07-24.
5. `SEO-INDEX-404-001` — классификация выполнена 2026-08-04: три `/index.html`
   являются алиасами главной и требуют отдельного точного redirect-релиза;
   назначение `/to.html` и `/overviews.html` не подтверждено, поэтому
   произвольные redirect не готовить.
6. `SEO-INDEX-DISCOVERED-001` — точная выгрузка и первичная сверка выполнены
   2026-08-04: все 23 URL существуют и входят в sitemap; следующий пакет —
   выборочное усиление внутренних ссылок и ценности кейсов, затем контрольный
   URL Inspection.
7. `SEO-LEGACY-MOBILE-001` — проверить старый URL `/mobile/index.html`, который
   продолжает получать показы и клик, и подготовить точный redirect на наиболее
   релевантную современную страницу.
8. `SEO-CANONICAL-001` — отдельным Nginx-релизом сделать non-`www` → `www`
   redirect с сохранением path и query string.
9. `SEO-GROWTH-CLUSTER-001` — улучшать страницы под уже наблюдаемые запросы:
   автоматизация парковки, автоматизированные парковочные системы, платная
   парковка, шлагбаумы и оборудование. Новые материалы создавать только после
   проверки, что существующая страница не закрывает интент.
10. После накопления данных связать поисковую страницу входа с CTA, диалогом
    AI-консультанта, формой и обработанным лидом в GA4 и Яндекс Метрике.

## 10. Критерий завершения SEO-OPS-001

Этап закрывается, когда:

- properties зарегистрированы и права подтверждены либо документирована
  причина использования другого владельца/аккаунта;
- получены агрегированные данные обоих кабинетов либо документирована причина
  отсутствия одного из них или ещё не накопленной истории;
- зафиксированы indexed/excluded и причины;
- проверены sitemap и выбранные canonical;
- сняты запросы и страницы входа за доступные периоды;
- отделены брендовые и небрендовые запросы;
- назначены владельцы кабинетов;
- создан приоритизированный backlog, основанный на данных, а не на
  предположениях;
- roadmap и changelog обновлены по итогам принятого блока.

## 11. Контрольный срез 2026-08-04

### 11.1. Google Search Console

Property: domain property `роспарк.рф`.

Данные эффективности доступны за период с 22.07.2026 по 02.08.2026; интерфейс
обновлён за три часа до проверки:

| Показатель | Значение |
|---|---:|
| Клики | 11 |
| Показы | 250 |
| CTR | 4,4% |
| Средняя позиция | 18,1 |
| Запросов в таблице | 45 |
| Страниц в таблице | 49 |

Первые наблюдаемые небрендовые запросы:

- `автоматизация парковок` — 1 клик, 9 показов;
- `автоматизация парковки` — 0 кликов, 5 показов;
- `автоматизация автопарков` — 0 кликов, 5 показов;
- `система автоматизации парковок` — 0 кликов, 4 показа;
- `автоматизированная парковочная система` — 0 кликов, 4 показа;
- `шлагбаум для парковки` — 0 кликов, 3 показа.

Страницы с наибольшим числом показов:

- `/` — 7 кликов, 125 показов;
- `/oborudovanie/shlagbaumy` — 1 клик, 26 показов;
- старый non-`www` `/mobile/index.html` — 1 клик, 19 показов;
- `/stati/raspoznavanie-nomerov-dlya-parkovki` — 2 клика, 13 показов;
- `/keysy/elektronika-na-presne` — 0 кликов, 13 показов.

Индексирование, последнее обновление 24.07.2026:

| Состояние | URL |
|---|---:|
| Проиндексировано | 57 |
| Не проиндексировано | 55 |
| Вариант страницы с canonical | 24 |
| Не найдено (404) | 5 |
| Страница с переадресацией | 2 |
| Копия без выбранного пользователем canonical | 1 |
| Обнаружена, не проиндексирована | 23 |

Sitemap `https://www.роспарк.рф/sitemap.xml` обработан 02.08.2026 со статусом
«Успешно», обнаружено 78 страниц и 0 видео. Для мобильных устройств и ПК пока
недостаточно полевых данных Core Web Vitals за 90 дней; это не означает ни
хороший, ни плохой результат и требует отдельной лабораторной проверки.

#### 11.1.1. Точные URL со статусом 404

Read-only выгрузка примеров Search Console 2026-08-04 показала:

```text
https://роспарк.рф/index.html
https://www.роспарк.рф/index.html
http://роспарк.рф/index.html
https://роспарк.рф/to.html
http://роспарк.рф/overviews.html
```

Классификация:

- три варианта `/index.html` являются техническими алиасами главной страницы;
  после отдельного согласования им нужен точный `301` на canonical `/`;
- `/to.html` и `/overviews.html` отсутствуют в текущем коде и внутренних
  ссылках; назначать им произвольную целевую страницу нельзя. До нахождения
  исторического содержания или подтверждённой замены корректнее сохранить
  `404`;
- изменение redirect-правил относится к отдельному Nginx-релизу с backup,
  `nginx -t`, smoke и rollback; текущий SEO-пакет production не меняет.

#### 11.1.2. Точные URL «Обнаружена, не проиндексирована»

Search Console показывает 23 URL, у всех в колонке последнего сканирования —
«Отсутствует»:

```text
/demo
/keysy/amaks-otel-kazan
/keysy/arktika
/keysy/burgas-sochi
/keysy/chkalovskaya
/keysy/depo3vokzala
/keysy/elma-kuryanovo
/keysy/galereya-rasskazovka
/keysy/gorizont-rostov
/keysy/hey-balashiha
/keysy/hey-luber
/keysy/mosflim
/keysy/odipark
/keysy/petrovsky
/keysy/plazma-murmansk
/keysy/pyatnicki
/keysy/ryabovskaya-manufaktura
/keysy/spar-chertanovskaya
/keysy/spar-elevatornaya
/keysy/spar-yaroslavskaya
/keysy/triumphlni
/privacy
/resheniya/sravnenie-podhodov
```

Сверка с кодом подтвердила: все 23 URL существуют, имеют осмысленное
назначение и входят в sitemap. В группе находятся 20 опубликованных кейсов,
индексируемый demo-хаб, юридическая страница и обзор сравнения подходов.
Следовательно, это не пакет битых URL и не основание массово удалять страницы.

Следующее действие:

1. структурная проверка всех 20 кейсов выполнена: у каждого есть обложка, три
   метрики, три FAQ, answer-first блок и содержательная часть;
2. для первого выборочного усиления связаны шесть подтверждённых кейсов:
   `depo3vokzala`, `gorizont-rostov`, `spar-chertanovskaya`, `mosflim`,
   `ryabovskaya-manufaktura` и `elma-kuryanovo` — из соответствующих страниц
   решений для торговых центров, бизнес-центров и складских комплексов;
3. URL Inspection 2026-08-05 выполнен без запроса переиндексации: `/demo` и
   `/keysy/depo3vokzala` пока неизвестны Google, а
   `/resheniya/sravnenie-podhodov` обнаружена через sitemap, но ещё не
   просканирована; повторить после обработки обновлённого sitemap;
4. `/privacy` оставить доступной и в sitemap как страницу доверия, но не
   считать её SEO-страницей роста;
5. не запускать «Проверить исправление», пока не опубликован согласованный
   пакет и не проверен реальный HTTPS-контур.

Массовая перелинковка всех кейсов и искусственное увеличение текста не
требуются. Следующая редакционная итерация определяется по результатам нового
обхода и фактическим поисковым данным после публикации пакета.

#### 11.1.3. Разметка товаров и merchant listings

Read-only проверка Search Console 2026-08-04 показала две связанные группы:

- Product snippets: 11 страниц без `offers`, `review` или `aggregateRating`;
- Product snippets: 3 предложения без подтверждённой цены;
- Merchant listings: 3 предложения без `price`, политики возврата и доставки.

Локальная причина подтверждена: восемь опубликованных карточек оборудования и
каталог объявляют элементы как `Product`, хотя на сайте нет публичных
подтверждённых цен, отзывов и рейтингов. Значение `priceFrom: 0` не является
ценой и не должно превращаться в `Offer`.

Опубликованное безопасное решение `SEO-INDEXING-002`:

1. не добавлять выдуманные цены, отзывы, рейтинги, доставку или возврат;
2. сохранить страницы оборудования, canonical, FAQ, хлебные крошки и
   технические характеристики;
3. описывать карточки как информационные `WebPage` об оборудовании, а каталог —
   как `CollectionPage` / `ItemList` страниц;
4. вернуть `Product` / `Offer` только после появления подтверждённых публичных
   коммерческих данных;
5. публичный JSON-LD проверен; проверку исправления в Search Console запускать
   только после того, как Google переобработает обновлённые страницы.

### 11.2. Яндекс Вебмастер

- критических ошибок диагностики нет;
- показаны 2 рекомендации;
- найдено 0 дублей `title` и 2 страницы с одинаковым `description`;
- дубль относится к `/oborudovanie/shlagbaum-rospark-3` и
  `/oborudovanie/shlagbaum-rospark-4`;
- в наблюдаемой таблице запросов есть `автоматизированные парковочные системы`
  — 54 показа и 3 клика, `автоматизация парковки` — 47 показов и 2 клика,
  `автоматизация платной парковки` — 4 показа и 3 клика;
- робот продолжает добавлять новые страницы: 04.08.2026 добавлена
  `/vozmozhnosti/onlain-oplata`, а страница статьи о парковке торгового центра
  была переобойдена и изменила состояние в поиске.

Первое локальное исправление `SEO-DESCRIPTION-001` подготовлено в ветке
`fix/site-20260804-seo-baseline-1`. До отдельного release-разрешения production
не изменяется и переобход страниц в кабинете вручную не запускается.

Дополнительная URL-prefix property `https://www.роспарк.рф/` подготовлена к
HTML-подтверждению. Verification-файл опубликован и отвечает `200`, но
read-only проверка 2026-08-05 показывает статус «Права на сайт не
подтверждены». Нажатие «Подтвердить права» остаётся отдельным внешним
действием.
