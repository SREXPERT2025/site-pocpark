# AI-WIDGET-0 — пакет знаний и правил

Дата подготовки: 2026-07-27, production-дополнение 2026-07-28
Статус: production-контур опубликован; latency/quality hardening прошёл QA в
отдельном локальном release candidate и ещё не переключён в production.

## Назначение

Этот каталог фиксирует первый этап `AI-WIDGET-0`:

- разрешённые источники знаний;
- реестр проверяемых утверждений;
- кандидат FAQ;
- правила диалога, согласия, хранения и передачи лида;
- блокирующие вопросы до закрытого технического пилота.

Пакет знаний дополнен UI/API-каркасом и локальным gateway. По умолчанию они
выключены feature flag. На 2026-07-27 владелец отдельно разрешил включить их
на коротком Mac Studio preview. На 2026-07-28 директор подтвердил подготовку
полноценного production-контура: публичный UI, реальный lead registry, журнал,
MAX outbox и отдельный Mac Studio gateway. Production cutover и контрольная
заявка с подтверждённой доставкой в MAX выполнены 28.07.2026.

## Состав

1. `WIDGET_KB_V1_SOURCES.md` — allowlist источников и порядок их приоритета.
2. `WIDGET_CLAIM_LEDGER_V1.md` — что агент может утверждать, что требует
   оговорки, а что запрещено обещать.
3. `WIDGET_FAQ_V1_APPROVED.md` — короткие ответы, утверждённые как точные
   шаблоны только для закрытого пилота.
4. `WIDGET_DATA_AND_LEAD_POLICY_V1.md` — разделение справочного диалога,
   аналитики и передачи обращения.
5. `WIDGET_OWNER_DECISIONS_20260727.md` — подтверждённые директором факты,
   обязательные поля, retention и граница закрытого пилота.
6. `WIDGET_AGENT_PROFILE_V1.md` — ограниченный профиль закрытого агента.
7. `WIDGET_CASCADE_V3_QA_20260727.md` — полный QA, найденное отклонение,
   исправление и scoped-регрессии.
8. `AI_WIDGET_1_PREVIEW_ACCEPTANCE_20260727.md` — фактическая приёмка первого
   read-only preview.
9. `AI_WIDGET_2T_PREVIEW_ACCEPTANCE_20260728.md` — чек-лист полного
   synthetic-контура с локальным журналом и тестовой карточкой.
10. `WIDGET_PRODUCTION_PROFILE_V1.md` — отдельный профиль публичного
    AI-консультанта без preview-инструкций.
11. `AI_WIDGET_PRODUCTION_ACCEPTANCE_20260728.md` — фактическая production
    приёмка, включая первую заявку и MAX delivery evidence.
12. `AI_WIDGET_LATENCY_QUALITY_V1_20260728.md` — диагностика задержек,
    отклонение небезопасной 8B-модели и локальный hardening candidate.
13. `WIDGET_RESPONSE_KNOWLEDGE_V1.md` — утверждённые сравнительные ориентиры
    для идентификации, очередей, гостевого доступа и первичного подбора.

Изолированный повторный тест запускается через
`scripts/ai_widget_cascade_v3_adapter.py`. Адаптер:

- проверяет SHA-256 старого test-only движка;
- читает соседний AI-проект без записи;
- принимает актуальный FAQ и пакет знаний из этого репозитория;
- требует имя для тестовой карточки;
- блокирует денежный ориентир в ответе на запрос цены, даже если сумма была
  предложена самим посетителем;
- разрешает только loopback Ollama;
- запрещает сохранять результаты внутрь рабочего `POCPARK_AI`;
- формирует отдельные `cascade_v3_summary.md` и
  `cascade_v3_validation.json`, используя актуальные имена полей результата;
- не включает MAX, реальный реестр или рабочий OpenClaw.

После полного прогона
`scripts/review_ai_widget_cascade_v3_results.py` формирует обезличенную очередь
ручного QA. В неё попадают lead/security, цены, гарантии, совместимость,
диагностика, границы знаний и любые автоматические ошибки. Телефоны и email в
отчёте маскируются.

## Локальные gateway

`scripts/run_ai_widget_pilot_gateway.py`:

- слушает только `127.0.0.1`;
- требует отдельный bearer secret длиной не менее 32 символов;
- загружает checksum-pinned cascade v3 и утверждённый FAQ read-only;
- точные FAQ, security и границы знаний обрабатывает детерминированно;
- для остальных вопросов разрешает только loopback Ollama и
  `qwen3.6:27b`;
- прогревает модель до readiness и удерживает её загруженной до двух часов;
- буферизует модельный ответ до Fact Gate и не отдаёт опасный черновик
  браузеру;
- не имеет инструментов CRM, MAX, файловой системы, браузера или оборудования;
- production gateway журналирует request ID, маршрут, HTTP-статус и
  длительность; hardening candidate дополнительно пишет только обезличенные
  UTC/TTFT/load/prompt/eval/token metrics без вопросов и ответов;
- site-side журнал закрытого теста отдельно сохраняет полный synthetic-диалог
  максимум на семь дней.

Один и тот же ограниченный gateway поддерживает два явно разделённых режима:

- preview на `127.0.0.1:8787`;
- production на `127.0.0.1:8788`.

Production-процесс использует отдельные профиль, env, bearer secret и launchd
job. До VPS он доступен только через закрытый HTTPS server-to-server канал.

Контракт проверяется командой:

```text
npm run test:ai-widget-gateway
```

Gateway установлен как отдельная фоновая служба Mac Studio. Preview и
production используют разные процессы и секреты. Результаты приёмки:
`AI_WIDGET_1_PREVIEW_ACCEPTANCE_20260727.md` и
`AI_WIDGET_PRODUCTION_ACCEPTANCE_20260728.md`.

Операционный runbook:
`docs/deployment/AI_WIDGET_PREVIEW_MAC_STUDIO.md`.

Production runbook:
`docs/deployment/AI_WIDGET_PRODUCTION_VPS_MAC_STUDIO.md`.

## Статусы

- `APPROVED_SOURCE` — опубликованный и принятый источник сайта.
- `CONDITIONAL` — сценарий возможен, но зависит от конфигурации объекта.
- `OWNER_REVIEW` — формулировка требует отдельного подтверждения владельца.
- `FORBIDDEN_PROMISE` — агент не имеет права подтверждать это как факт.
- `DRAFT_EXCLUDED` — черновик не входит в базу знаний.

## Порядок применения

1. Сначала ищется точный FAQ-шаблон.
2. Затем разрешённые фрагменты `widget-kb-v1`.
3. Каждый подготовленный ответ проверяется по claim ledger.
4. Если подтверждённого факта нет, используется граница знаний.
5. Контакт запрашивается только после намерения передать обращение; тестовый
   сбор выполняется детерминированным site-side сценарием, а не свободным
   ответом модели.
6. Сообщение о сохранении или уведомлении менеджера разрешено только после
   успешного результата узкого серверного инструмента.

## Граница текущего блока

- рабочий OpenClaw `main` не изменяется;
- preview не считается публичным production и не делит с ним секрет;
- production использует отдельный `/api/ai-widget/lead`, а не свободный
  доступ модели к `/api/lead`;
- первая production-отправка в MAX выполнена и подтверждена outbox, worker и
  read-only MAX API;
- последующие изменения публикуются только после target SHA, backup, staging,
  readiness и отдельного подтверждения cutover.
