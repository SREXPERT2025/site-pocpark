# РОСПАРК — post-release SEO и analytics check

Дата: 2026-08-05
Режим: read-only, без ручной переиндексации, лидов и сообщений в MAX

## Production

- SHA: `c57887f81f53408d273e3b978184c68e09e06c1a`;
- публичный sitemap: 80 URL;
- `/parkovka` и `/parkovka-pod-klyuch` присутствуют по одному разу;
- Yandex verification-файл отвечает `200`.

## Google Search Console

- property `роспарк.рф` доступна;
- сводка: 57 проиндексированных и 55 неиндексированных URL;
- актуальный `https://www.роспарк.рф/sitemap.xml` повторно отправлен 2026-08-05;
  интерфейс подтвердил «Файл Sitemap отправлен», переобработка 80 URL ожидается;
- `/demo`: URL неизвестен Google, ссылающийся sitemap пока не указан;
- `/keysy/depo3vokzala`: URL неизвестен Google, новая внутренняя ссылка и
  sitemap ещё не учтены;
- `/resheniya/sravnenie-podhodov`: обнаружена через sitemap, но не
  просканирована;
- ручной запрос индексирования и «Проверить исправление» не запускались;
- старые Product/Merchant ошибки остаются в сводке до нового обхода.

Повторный read-only срез 2026-08-05:

- performance-отчёт обновлён 5 часов назад, но содержит данные только по
  2 августа: 11 кликов, 250 показов, CTR 4,4%, средняя позиция 18,1;
- индексный отчёт всё ещё датирован 24 июля: 57 проиндексированных и 55
  неиндексированных URL, поэтому он ещё не отражает новый sitemap и релизы;
- старый `https://роспарк.рф/mobile/index.html` получил 1 клик и 19 показов,
  хотя текущий ответ — 404; локально подготовлен точечный 308 на каноническую
  главную с сохранением query string;
- `/parkovka` и `/parkovka-pod-klyuch` пока не появились среди URL с
  поисковыми показами — после публикации прошло недостаточно времени.

## Яндекс Вебмастер

- сайт `https://www.роспарк.рф` присутствует в аккаунте;
- 2026-08-05 дополнительная URL-prefix property успешно подтверждена через
  опубликованный HTML-файл;
- пользователь `rospark.adver` получил роль «Владелец», метод проверки —
  «Файл в корневом каталоге», код `576fde8bc91c6899`;
- при подтверждении property настройки sitemap и индексирования не менялись;
- 2026-08-05 для sitemap запущен разрешённый ручной переобход: доступный лимит
  уменьшился с 10 до 9, кнопка стала неактивной; новая версия с 80 URL ожидает
  обработки.

## Аналитика

- Метрика `110980303` видит production-трафик и обе посадочные страницы;
- на момент проверки за день: `/parkovka` — 6 просмотров,
  `/parkovka-pod-klyuch` — 5 просмотров;
- в текущем Yandex login обзор счётчика доступен, а прямой экран управления
  целями возвращает отсутствие прав для `rospark-adver` и предлагает вход
  владельцем `radicom`; настройки целей не изменялись;
- в GA4 ключевое событие `rospark_ai_lead_handoff` создано, но поток за
  последние 28 дней пока не обнаружен; список недавних событий содержит 0
  записей;
- код отправляет privacy-safe события `rospark_ai_promo_view`,
  `rospark_ai_promo_click`, `rospark_ai_chat_open`,
  `rospark_ai_quick_question_click`, `rospark_ai_first_message_sent`,
  `rospark_ai_engaged_chat` и `rospark_ai_lead_handoff` только после согласия;
- контрольную заявку и тестовое сообщение AI не создавали.

Повторный read-only аудит GA4 2026-08-05 подтвердил:

- Measurement ID веб-потока совпадает с production:
  `G-3Z9KNN3MMK`, stream ID `15338809658`;
- GA4 показывает «за последние 48 часов трафик не регистрировался» и 0
  недавних событий, то есть это не только задержка конкретной AI-цели;
- локальный loader передавал команды как обычные массивы, тогда как официальный
  контракт `gtag.js` требует native `Arguments` envelope;
- локально подготовлено исправление загрузчика и regression-test; реальное
  контрольное событие в production не отправлялось;
- текущая сессия Метрики авторизована как `rospark-adver`, которому доступ к
  счётчику `110980303` запрещён; для численного среза нужен владелец `radicom`.

## Следующий пакет

1. Выполнено в `2f8b2b2`: `/parkovka` и `/parkovka-pod-klyuch` отдают
   `index, follow`, корректный canonical и присутствуют в sitemap.
2. Дождаться переобработки повторно отправленного sitemap и повторить те же три
   URL Inspection.
3. Получить доступ к настройкам целей Метрики либо войти владельцем счётчика.
4. Подтвердить поступление AI-событий на реальном обезличенном трафике.
5. Собрать карту «поисковый запрос → существующая целевая страница».
6. Выполнено в `c57887f`: опубликованы WebP `/parkovka`, Service JSON-LD двух
   лендингов и FAQ JSON-LD страницы «Парковка под ключ».
7. Следующим накопительным пакетом опубликовать адаптивную доставку изображений
   `/parkovka-pod-klyuch`, затем повторить Lighthouse на публичном HTTPS.

## Лабораторный performance baseline

PageSpeed Insights, mobile, Lighthouse 13.4.1, 2026-08-05:

| Страница | Performance | LCP | TBT | CLS | SEO |
|---|---:|---:|---:|---:|---:|
| `/` | 95 | 2,9 с | 0 мс | 0 | 100 |
| `/parkovka` | 91 | 3,2 с | 0 мс | 0 | 69 |
| `/parkovka-pod-klyuch` | 91 | 3,3 с | 0 мс | 0 | 69 |

SEO 69 у обеих посадочных вызван не качеством metadata, а фактическим
`noindex`. Для `/parkovka` Lighthouse отдельно оценивает потенциальную экономию
изображений примерно в 1 854 КиБ при общем объёме загрузки около 5 696 КиБ.
Основные исходные PNG: `hero-puzzle-terminals.png` (около 1,9 МБ),
`object-business-center-w-plaza.png` (около 1,36 МБ),
`case-poklonka-parking-entry.png` (около 1,26 МБ) и
`object-retail-eurospar.png` (около 1,15 МБ). Оптимизировать только с
визуальным сравнением; структуру лендинга и AI-виджет не менять.

## Выпущенный пакет `SEO-PERF-GEO-001`

- 13 реально используемых PNG/JPG `/parkovka` преобразованы в WebP: суммарно
  `10 231 459` → `933 638` байт;
- весь raster-набор, на который ссылается bundle, занимает `1 414 438` байт;
- оригиналы сохранены, внешний вид выборочно проверен;
- добавлен regression-test: только WebP, каждый файл не более 250 КБ, весь
  raster payload не более 2 МБ;
- подготовлены подтверждённые видимым текстом Service JSON-LD двух лендингов и
  FAQ JSON-LD страницы «Парковка под ключ» без цен, Offer, рейтингов и отзывов;
- пакет опубликован в production SHA
  `c57887f81f53408d273e3b978184c68e09e06c1a`; публичные HTTPS-проверки,
  canonical, robots и JSON-LD пройдены.

## Post-release Lighthouse и responsive-image release

Lighthouse 13.4.1, mobile simulated throttling, production HTTPS после
`c57887f`:

| Страница | Performance | LCP | TBT | CLS | SEO | Передано |
|---|---:|---:|---:|---:|---:|---:|
| `/parkovka` | 92 | 3,20 с | 38 мс | 0 | 100 | 967 КБ |
| `/parkovka-pod-klyuch` | 93 | 3,25 с | 0 мс | 0 | 100 | 589 КБ |

По сравнению с baseline общий объём `/parkovka` уменьшился примерно с
5,70 МБ до 0,97 МБ. LCP остался около 3,2 с; следующий резерв — не структура
страницы, а доставка оставшихся изображений и общий клиентский JavaScript.

В production SHA `2fc363e` опубликовано включение responsive Next Image для
всех девяти изображений `/parkovka-pod-klyuch`. Проверка при 1440 и 390 px подтвердила
адаптивный `srcset`, отсутствие горизонтального переполнения и сохранение
компоновки. Локальный Lighthouse показал 399 КБ вместо 589 КБ; аудит больше не
предлагает экономию image delivery.

В тот же накопительный release вошли native GA4 command envelope и постоянный
308 `/mobile/index.html` на каноническую главную с сохранением query string.
Публичный HTTPS smoke, read-only AI status, SQLite quick check и no-send gate
пройдены; создано 0 лидов и отправлено 0 сообщений в MAX.

Немедленный read-only срез GA4 после выпуска показывает:

- `rospark_ai_lead_handoff` отмечен как key event;
- недавних событий пока 0;
- для key events всё ещё показано «Поток данных не обнаружен»;
- интерфейс GA4 указывает, что стандартный отчёт может обновляться до 24 часов.

Следующая проверка — после окна обработки, без искусственной заявки или
контрольного сообщения AI.
