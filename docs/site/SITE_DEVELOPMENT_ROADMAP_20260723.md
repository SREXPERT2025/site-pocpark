# РОСПАРК — актуальный план развития сайта

Дата актуализации: 2026-08-05
База аудита и текущий production release:
`1351b9334ef9e5a6017b4852783dd70e40d94c71`
Production-ветка: `release/demo-production-ready-20260723`
Статус: текущий источник правды после выпуска двух рекламных лендингов,
site-wide AI-аналитики, SEO baseline, AI context hotfix и объединённого
`SEO-INDEXING-002` и индексируемого режима двух утверждённых лендингов. Sitemap
повторно отправлен в Google Search Console и поставлен на переобход в Яндекс
Вебмастере 2026-08-05. Первый performance/GEO-пакет и следующий накопительный
пакет responsive image delivery, GA4 и legacy redirect опубликованы. Также
опубликованы только подтверждённые редакционные даты и оптимизированное
Hero-видео главной. Следующий этап — дождаться обработки GA4 и поисковых
кабинетов, зафиксировать первый не-QA аналитический срез после 5 августа и на
его основе начать `GEO-CONTENT-001` без неподтверждённых коммерческих
утверждений.

Предыдущие планы сохранены как исторические документы:

- `docs/site/SITE_DEVELOPMENT_ROADMAP_20260707.md`;
- `docs/site/IMPROVEMENT_PLAN_20260706.md`;
- `docs/site/ARTICLES_SEO_GEO_BACKLOG_20260706.md`.

Текущий контентный backlog:
`docs/site/CONTENT_SEO_GEO_BACKLOG_20260723.md`.

Послерелизный контроль и evidence gate:

- `docs/site/SEO_GEO_MEASUREMENT_MATRIX_20260805.md`;
- `docs/site/GEO_CONTENT_SOURCE_INPUT_20260805.md`;
- `docs/site/GEO_ENTITY_SOURCE_LEDGER_20260805.md`.

Production, публикация контента, изменение инфраструктуры, подключение внешних
сервисов и реальные отправки выполняются только после отдельного подтверждения.

## 1. Текущее состояние

### 1.1. Production

На 2026-08-05 подтверждено:

- основной сайт и Demo Release v1 опубликованы;
- production работает под Node.js 22 и Next.js 14.2.35;
- reverse proxy — Nginx;
- release SHA сайта — `1351b9334ef9e5a6017b4852783dd70e40d94c71`;
- `/`, `/demo`, `/demo/gostevaya-zayavka`, `/demo/web-skidki` и
  `/demo/vladelec-parkovki` отвечают `200`;
- `robots.txt` открыт для обхода и содержит ссылку на sitemap;
- публичный sitemap содержит 80 URL; актуальный sitemap повторно отправлен в
  Search Console и Яндекс Вебмастер 2026-08-05, его переобработка ожидается;
- `/demo` индексируем и включён в sitemap;
- внутренние demo-кабинеты и публичные token-страницы закрыты `noindex`;
- metadata и self-canonical юридических страниц опубликованы;
- на релевантных коммерческих страницах опубликованы входы в три
  demo-сценария;
- Яндекс Метрика `110980303` опубликована через consent-gated loader;
- Google Analytics 4 `G-3Z9KNN3MMK` опубликован через отдельный
  consent-gated loader без GTM;
- публичный AI-консультант, журнал диалогов и передача согласованной заявки в
  lead registry/MAX работают в production;
- будущие MAX-отправки сохраняют provider message ID, destination ID и время
  принятия провайдером;
- три demo-статьи и обновлённые обложки опубликованы;
- публичные рекламные лендинги `/parkovka` и
  `/parkovka-pod-klyuch` опубликованы и отвечают `200`;
- прежний `/puzzle2` постоянно перенаправляется на
  `/parkovka-pod-klyuch`;
- архивные `/proshche`, `/puzzle`, `/test2`, `/v4-1`, `/v4-2` сохранены в
  Git, но закрыты в production ответом `404`;
- лендинги доступны из главной, навигации, футера и релевантных разделов;
- формы лендингов используют общий lead registry, а AI-промо открывает общий
  production-виджет с безопасным контекстом выбранной задачи;
- события лендингов и AI-воронки подключены к общей privacy-safe аналитике;
- внешний browser smoke подтвердил отсутствие Метрики до согласия, pageview
  после согласия и отключение после отзыва;
- фактическое состояние production зафиксировано в
  `docs/production/PRODUCTION_STATE_2026_07_23.md`.

### 1.2. Коммерческий сайт и SEO/GEO-основа

Выполнено:

- canonical URL и metadata ключевых страниц;
- хабы `/resheniya`, `/vozmozhnosti`, `/oborudovanie`, `/keysy` и `/stati`;
- Organization, CaseStudy, Article, FAQ, Breadcrumb и ItemList JSON-LD;
- 12 опубликованных статей;
- 30 опубликованных кейсов;
- 6 опубликованных страниц возможностей;
- 8 опубликованных карточек оборудования;
- answer-first и FAQ в статьях, кейсах и карточках оборудования;
- draft-контент исключён из каталогов, sitemap, static params и прямого доступа;
- `/quiz` исключён из sitemap и закрыт `noindex, follow`;
- формы используют явное согласие и передают контекст заявки и UTM;
- настроены Email, Telegram и внутренний MAX как каналы доставки `/api/lead`;
- исправлены основные проблемы H1, семантики, keyboard navigation, focus,
  responsive images и недостоверно сильных формулировок.

### 1.3. Demo Release v1

Выполнено:

- единый каталог demo-сценариев `/demo`;
- гостевые заявки арендатора;
- публичная ссылка и QR-код;
- отмена ожидающей заявки;
- серверные cookie-сессии и изоляция пользовательских данных;
- сценарий «Оплата парковки гостей»;
- кабинет владельца парковки;
- сводка, арендаторы, гостевые заявки, оплаты и общий журнал операций;
- предыдущий завершённый и текущий периоды;
- детерминированная синтетическая история;
- добровольное отдельное согласие на обратную связь;
- идемпотентное сохранение demo feedback lead;
- MAX, WhatsApp и copy/share-сценарии.

## 2. Главные расхождения и риски

### Отложено. PROD-DATA-OPS — вынести SQLite из Git checkout

Статус: перенос отложен по решению владельца от 2026-07-23. Локальный аудит,
read-only VPS preflight и свежий проверенный online backup завершены; план
подготовлен в `docs/production/PROD_DATA_OPS_PLAN_20260723.md`. Текущая база
остаётся на старом пути, maintenance window не выполнялось.

Текущая production-база находится в:

```text
/var/www/rospark-site/.data/guest-requests.sqlite
```

Целевой каталог:

```text
/var/lib/rospark-demo/guest-requests.sqlite
```

Перенос выполнять отдельным окном:

- определить фактического process user;
- создать SQLite `.backup`;
- проверить `integrity_check` и `foreign_key_check`;
- остановить запись на время переключения;
- перенести базу с учётом WAL/SHM;
- обновить `DEMO_REQUESTS_DB_PATH`;
- проверить права и владельца;
- выполнить полный smoke;
- сохранить rollback.

Обычное копирование работающей SQLite/WAL не считается безопасным переносом.

Вернуть задачу в активный план перед заменой production checkout, изменением
deploy-схемы или следующим крупным production-релизом.

### P0. RUNTIME-001 — обязательный Node.js 22

Проект закреплён на Node.js 22 через `engines` и `.nvmrc`. Локальный запуск под
Node.js 26 воспроизвёл `500` в demo API при обращении к нативному
`better-sqlite3`; под Node.js 22 те же Stage B/Stage C тесты прошли.

Нужно:

- добавить проверку `node --version` в каждый preflight;
- не запускать build/start при несовпадении major-версии;
- документировать rebuild нативного модуля после смены Node ABI;
- использовать одинаковую версию Node для `npm ci`, build и runtime.

### P0. LEAD-OPS-002 — замкнуть обработку demo-лида

Статус: выполнен и принят в production 2026-07-24.

Read-only аудит 2026-07-24 подтвердил два несвязанных контура:

- `/api/lead` доставляет Email / Telegram / MAX, но не сохраняет операционный
  реестр;
- `/api/demo/feedback-leads` сохраняет запись на 30 дней, но не создаёт
  outbox, не уведомляет менеджера и не меняет status;
- schema не содержит `assigned`, хотя этот переход записан в roadmap;
- очистка истёкших feedback leads выполняется opportunistically при следующей
  demo-операции, а не отдельным гарантированным job.

Decision packet:
`docs/site/LEAD_OPS_002_DECISION_20260724.md`.

По результатам аудита реализованы и опубликованы:

- отдельная schema migration `lead_registry_foundation`;
- `lead_records`, `lead_submissions`, `lead_status_events`;
- idempotency по `submission_id`;
- 24-часовая от последней заявки привязка повторной submission к открытому
  лиду;
- строгие status transitions и подтверждённые outcomes;
- retention 60/30 дней и явная cascade cleanup;
- transactional outbox для MAX/Email;
- lease, retry/backoff, dead state и безопасные error codes;
- оба API подключены к registry за выключенным feature gate;
- one-shot worker отдельно закрыт вторым выключенным gate;
- защищённый `/admin/leads` с персональными ролями Андрея и Сергея;
- фильтры, сводка, status workflow и контролируемый CSV export;
- append-only audit доступа, export, статусов и удаления без PII;
- ручное удаление только для директора с двойным подтверждением;
- admin `noindex`/`no-store`, запрет iframe и исключение Метрики;
- автоматические и локальные browser/API smoke.

Production L4:

- CLI теперь загружает `.env.production` явно;
- retention cleanup отделена от внешней отправки;
- env configurator обновляет только allowlisted lead-ключи атомарно, с backup
  и без вывода секретов;
- подготовлены systemd templates worker/cleanup с общим `flock`;
- выполнены backup, staged release, acceptance и сохранён rollback;
- registry работает вне Git checkout в
  `/var/lib/rospark-leads/lead-registry.sqlite`;
- Андрей и Сергей вошли под персональными ролями;
- одна маркированная TEST-заявка один раз доставлена в MAX;
- подтверждён workflow `new → assigned → contacted → closed(test)`;
- outbox и cleanup systemd timers активны;
- production SHA:
  `61a4694bee55426e72bdfbb42008730c3cb2b444`.

Дальше задача переходит из разработки в эксплуатационный контроль: следить за
`failed/dead`, SLA первого контакта и сроками retention без ручного чтения
SQLite.

### P0. DOCS-OPS-001 — поддерживать один источник правды

Этот документ является текущим планом развития. После каждого принятого блока
нужно обновлять:

- этот roadmap;
- `docs/site/CHANGELOG.md`;
- `docs/production/PRODUCTION_STATE_2026_07_23.md`, если меняется production;
- соответствующий runbook;
- узкий backlog направления.

## 3. P1 — измерение SEO/GEO и конверсии

### SEO-OPS-001 — baseline индексации

Публичная read-only часть завершена 2026-07-23 и зафиксирована в:

`docs/site/SEO_GEO_BASELINE_20260723.md`.

Подтверждено:

- sitemap содержит 75 уникальных `www` URL, все отвечают `200`;
- в sitemap нет `noindex`;
- у всех sitemap-страниц есть title, description, один H1 и JSON-LD;
- внутренние demo-сценарии и `/quiz` корректно исключены из sitemap и закрыты
  `noindex`;
- HTTPS non-`www` доступен с `200`, хотя canonical указывает на `www`;
- `/privacy` и `/soglasie-na-obrabotku-personalnyh-dannyh` получили
  собственные metadata и self-canonical в production release
  `9ae9579c63dc8c3c7af96a1e46d87ee0081b56da`.

Первый фактический срез Google Search Console и Яндекс Вебмастера получен
2026-08-04 и записан в SEO baseline. Подтверждены 57 проиндексированных URL,
23 обнаруженных без обхода URL и ошибки товарной разметки оборудования.

Публичная выдача подтверждает только частичную обнаруживаемость сайта и не
заменяет данные кабинетов.

В текущих авторизованных аккаунтах 2026-07-23 созданы Domain property
`роспарк.рф` в Google и сайт `https://www.роспарк.рф/` в Яндексе. Обе DNS TXT
первоначально опубликованы на корневом host через `ns1.reg.ru` и `ns2.reg.ru`.
Google Domain property успешно подтверждена. Яндекс проверяет конкретный
`www`-host и при первой проверке не нашёл ключ, поскольку TXT на
`www.роспарк.рф` отсутствовала. Отдельная Yandex TXT на subdomain `www` уже
добавлена и видна на авторитетных и публичных DNS; права Яндекса также успешно
подтверждены. Начальный read-only срез показал:

- Google sitemap обработан со статусом «Успешно», обнаружено 75 страниц;
- отчёт индексирования Google всё ещё обрабатывается и просит повторить
  проверку приблизительно через день;
- Яндекс пока не сформировал свежие данные, текущих ошибок и массовых дублей не
  показывает;
- старая история обхода Яндекса относится к legacy URL 2024 года;
- до отправки в обоих кабинетах список sitemap был пуст.

`SEO-SITEMAP-SUBMIT-001` выполнен 2026-07-23:

- текущий публичный sitemap отправлен в Google и Яндекс;
- Google подтвердил отправку, а контроль 2026-07-24 показал успешную обработку
  и 75 обнаруженных страниц;
- Яндекс поставил файл в очередь на обработку со сроком до 1–2 недель;
- следующий контрольный срез Google — приблизительно через сутки;
- следующий контрольный срез Яндекса — после первого свежего обхода.

Verification TXT не удалять.

Связанные отдельные задачи:

- `SEO-WEBMASTER-ENROLL-001` — подключение и DNS-верификация кабинетов;
- `SEO-SITEMAP-SUBMIT-001` — отправка проверенного sitemap в оба кабинета;
- `SEO-META-LEGAL-001` — выполнен и опубликован 2026-07-24;
- `SEO-CANONICAL-001` — согласованный Nginx redirect non-`www` → `www`.

Опубликованный пакет `SEO-INDEXING-002`:

- HTML verification-файл дополнительной URL-prefix property Яндекса;
- `/parkovka` и `/parkovka-pod-klyuch` в sitemap;
- карточки оборудования остаются индексируемыми информационными страницами,
  но больше не заявляют Product/Merchant rich results без цены и отзывов;
- каталог сохраняет `CollectionPage` / `ItemList` без фиктивных Offer;
- выполнена выборочная структурная ревизия всех 20 кейсов: у каждого есть
  обложка, метрики, FAQ, answer-first блок и содержательная часть;
- шесть сильных кейсов естественно связаны с решениями для торговых центров,
  бизнес-центров и складских комплексов;
- production release `a736baf`, публичный HTTPS acceptance и no-send gate
  завершены 2026-08-05;
- дополнительная URL-prefix property `https://www.роспарк.рф/` подтверждена
  2026-08-05 через HTML-файл: `rospark.adver` получил роль «Владелец»;
- свежий обход и переобработка sitemap остаются отдельными последующими
  действиями.

Следующий блок после `SEO-INDEXING-002`:

1. выполнено release `2f8b2b2`: `/parkovka` и `/parkovka-pod-klyuch`
   переключены с временного `noindex` на `index, follow`, canonical и наличие
   обеих страниц в sitemap подтверждены по публичному HTTPS;
2. после публикации и свежего обхода оценить, какие из оставшихся кейсов
   действительно нуждаются в редакционном усилении, не переписывая их заранее;
3. продолжать естественную перелинковку только при появлении тематически
   подходящих мест в статьях и решениях;
4. актуальный sitemap повторно отправлен в Google и поставлен на переобход в
   Яндексе; после его обработки повторить URL Inspection. Предыдущая проверка:
   `/demo` и кейс `depo3vokzala` пока
   неизвестны Google, `/resheniya/sravnenie-podhodov` обнаружена через sitemap,
   но ещё не просканирована; повторить после обработки новой версии sitemap;
5. лабораторный baseline выполнен: главная — 95, обе посадочные — 91. Локальный
   кандидат оптимизации `/parkovka` уменьшает реально используемые raster assets
   примерно с 10,23 МБ до 0,93 МБ без изменения дизайна; требуется production
   release-gate и повторный PageSpeed замер;
6. начальная карта существующих страниц под наблюдаемые запросы создана без
   дублей и дорвеев; пересмотреть после свежего поискового среза.

### ANALYTICS-001 — подключить потребителя событий

Код создаёт privacy-safe события форм и гостевого demo. Яндекс Метрика работает
как первый прямой потребитель, production-счётчик `110980303` подтверждён.
Прямая GA4-интеграция опубликована 2026-07-28 с Measurement ID
`G-3Z9KNN3MMK` в накопительном production release `47529cc`; первое событие и
Realtime ожидают подтверждения после обработки Google.

Локальный статус `ANALYTICS-001-A`:

- подготовлен единый consent-gate для cookie banner и dispatcher;
- payload ограничен allowlist и удаляет query string из `source_page`;
- событиями покрыты формы, гостевые заявки, оплата парковки и owner cabinet;
- typecheck, lint, production build, privacy smoke и локальный browser smoke
  пройдены;
- внешний loader, ID счётчика и production не изменялись;
- подробный контракт и этапы зафиксированы в
  `docs/site/ANALYTICS_PLAN_20260723.md`.

Production-статус `ANALYTICS-001-B`:

- первым выбран прямой loader Яндекс Метрики без GTM/GA4;
- счётчик `110980303`, владелец Yandex ID `radicom`;
- ID вынесен в `NEXT_PUBLIC_YANDEX_METRIKA_ID`;
- loader запускается только после `accepted`;
- Webvisor, clickmap, e-commerce и передача title выключены;
- e-commerce и встроенный Yandex Tag Manager выключены в настройках счётчика;
- события форм и demo передаются через `reachGoal`;
- добавлена постоянная кнопка повторного открытия cookie-настроек;
- typecheck, lint, production build, privacy smoke и локальный consent browser
  smoke пройдены;
- `NEXT_PUBLIC_YANDEX_METRIKA_ID=110980303` добавлен в production environment
  до сборки;
- release `9ae9579c63dc8c3c7af96a1e46d87ee0081b56da` опубликован 2026-07-24;
- внешний consent smoke подтвердил корректную загрузку и отключение Метрики;
- browser console — без ошибок и предупреждений.

Production-расширение GA4:

- добавлен прямой `gtag.js` loader без GTM;
- в Google Analytics создан ресурс и веб-поток для
  `https://www.роспарк.рф`;
- Measurement ID `G-3Z9KNN3MMK` зафиксирован в `.env.example`;
- loader работает только после analytics consent, только на production-host и
  исключён из `/admin`;
- рекламное хранилище, Google Signals и персонализация рекламы выключены;
- pageview и существующие `rospark_*`-события не содержат query string и PII;
- автоматические history-based pageview отключены в улучшенной статистике GA4,
  чтобы не дублировать ручные SPA pageview;
- production environment и build содержат Measurement ID;
- production consent browser smoke и отсутствие GA4 в `/admin` подтверждены;
- остаётся проверить первое событие и Realtime после обработки Google до
  48 часов.

Статус `ANALYTICS-001-C`:

- в счётчике создано восемь ручных целей: `/demo`, открытие сценария,
  создание гостевой заявки, завершение попытки оплаты, детализация владельца,
  результат feedback-лида, успешная отправка формы и вход в demo/quiz;
- production browser smoke подтвердил `reachGoal`
  `rospark_demo_scenario_view` с параметром `demo_name` без PII;
- release `80d64da4b2cdd3b6af7f837709722db66702930d` устранил риск потери события
  при прямом открытии demo-страницы;
- внешний production smoke подтвердил ровно одну цель при hard load и одну при
  SPA-переходе, browser console без ошибок и предупреждений;
- release `c2a0e955b8747e3005da28e3fe9981f01fa45488` ограничил внешний loader
  Метрики production-host и исключил дальнейшую отправку localhost QA;
- внешний smoke после host guard release подтвердил публичную demo-страницу,
  `tag.js?id=110980303` и отсутствие ошибок консоли;
- после закрытия `LEAD-OPS-002` добавлена закрытая агрегированная
  сводка `received → assigned → contacted → closed`, повторов, источников и
  первого контакта за один рабочий час;
- агрегаты не содержат PII и не отправляются в Яндекс Метрику;
- lead-admin tests, lint, Node.js 22 build и локальный browser smoke пройдены;
- production release
  `26740a5a0fe485b6ff3427283f3461d4dddd22ba` выполнен 2026-07-25;
- production browser smoke подтвердил вход Андрея, правильные TEST-агрегаты,
  отсутствие PII, Метрики, публичного layout, overflow и console errors;
- событие `rospark_funnel_entry` для переходов с публичного контента в
  `/demo` и `/quiz` опубликовано в production release
  `89c045d79535169527347c40c438971fb560995d`;
- safe landing context ограничен фиксированной категорией страницы и не
  содержит полный URL, query/UTM, текст CTA или динамический slug;
- privacy smoke, typecheck, lint, Node.js 22 build и production-переходы в
  demo и quiz пройдены;
- создана восьмая ручная цель `Воронка — вход в demo/quiz`, ID `588884963`;
- два контрольных события выполнены без отправки форм и PII; после обработки
  Метрика зарегистрировала один целевой визит и четыре просмотра в нём, что
  подтверждает доставку цели;
- backup:
  `/root/rospark-backups/analytics-server-summary-20260725T115320Z`;
- funnel release backup:
  `/root/rospark-backups/analytics-funnel-20260725T125007Z`;
- подробности записаны в `docs/site/ANALYTICS_GOALS_20260724.md`;
- backup release:
  `/root/rospark-backups/analytics-host-guard-20260724T070217Z`.

Статус `ANALYTICS-001-D` на 2026-08-04:

- события просмотров, CTA, выбора задачи/функций, формы и AI-промо двух
  лендингов опубликованы;
- site-wide AI-воронка включает открытие чата, первое сообщение, вовлечённый
  диалог и handoff в рабочую заявку;
- payload ограничен вариантом лендинга, секцией, pathname и безопасными
  категориями выбора; текст вопроса, контакт и UTM не передаются;
- production code опубликован в `279d919`;
- создание четырёх новых ручных целей AI в интерфейсе Метрики и первые не-QA
  достижения ещё требуют подтверждения;
- в GA4 `rospark_ai_lead_handoff` следует пометить как основное key event,
  остальные AI-события оставить этапами воронки.

Статус `ANALYTICS-001-E` на 2026-08-05:

- native envelope команд `gtag.js` опубликован в production SHA `2fc363e`;
- `rospark_ai_lead_handoff` уже отмечен в GA4 как key event;
- немедленный read-only срез после выпуска показывает 0 недавних событий и
  «Поток данных не обнаружен»; сам интерфейс GA4 предупреждает о задержке
  стандартного отчёта до 24 часов;
- создавать искусственное событие, заявку или AI-сообщение для ускорения
  отчёта не стали;
- следующий gate — повторный read-only срез после окна обработки и отдельный
  доступ владельца счётчика Метрики `110980303`.

Нужно дальше:

- назначить резервного администратора и правила доступа;
- дождаться не-QA данных остальных шести целей и параметров `result`;
- после накопления данных проверить раздельную статистику
  `rospark_funnel_entry` по разрешённым параметрам `destination` /
  `landing_group`;
- создать/подтвердить четыре AI-цели Метрики и key event GA4 без изменения
  privacy-контракта;
- сравнивать `/parkovka` и `/parkovka-pod-klyuch` по успешной форме и AI lead
  handoff, а не только по CTA;
- собирать воронку
  `источник → контент → demo/quiz → consent → lead → обработка`;
- вести ежемесячный SEO/GEO/conversion dashboard; первый baseline создан в
  `docs/site/GROWTH_DASHBOARD_20260724.md`.

### CLAIMS-001 — реестр подтверждённых публичных утверждений

Для цен, сроков, количества объектов, географии, ответа менеджера, бесплатного
аудита, характеристик и интеграций нужен claim ledger:

```text
утверждение
→ публичные URL
→ источник
→ дата проверки
→ ответственный
→ срок следующей проверки
```

Не менять подтверждённые коммерческие условия автоматически.

## 4. P1 — коммерческая упаковка demo

### DEMO-GROWTH-001

- сохранить `/demo` как индексируемый коммерческий хаб;
- оставить внутренние кабинеты и token-страницы `noindex`;
- добавить переходы на `/demo` из релевантных решений и возможностей;
- подготовить сценарий показа для менеджера;
- подготовить короткое коммерческое описание трёх ролей;
- связать demo с источником/UTM и реальным lead registry;
- измерять завершение каждого сценария;
- не превращать demo в реальную команду оборудованию.

Production-статус 2026-07-24:

- подготовлен единый контекстный demo-callout с переходом в `/demo` и три
  сценария;
- callout подключён к странице бизнес-центров и релевантным страницам
  возможностей для арендаторов, гостей и оплаты;
- подготовлен внутренний сценарий коммерческого показа
  `docs/site/DEMO_SALES_WALKTHROUGH_20260723.md`;
- typecheck, lint, production build и desktop/mobile browser smoke пройдены;
- изменения опубликованы в release
  `9ae9579c63dc8c3c7af96a1e46d87ee0081b56da`;
- публичная страница бизнес-центров показывает единый callout и три
  scenario-ссылки;
- индексация внутренних кабинетов и demo API не изменялась.

### CONTENT-DEMO-001

Первые три SEO/GEO-кластера к demo:

1. статья о гостевой заявке;
2. статья об оплате парковки гостя за счёт арендатора;
3. статья об отчётности владельца парковки;
4. обновление страниц бизнес-центров, интеграций и гостевых клиентов;
5. контекстные ссылки на `/demo`, а не индексация внутренних кабинетов.

Read-only аудит 2026-07-25 зафиксирован в
`docs/site/CONTENT_DEMO_AUDIT_20260725.md`:

- demo-каталог, внутренние noindex-сценарии, коммерческие callout и
  production-аналитика уже готовы;
- отдельные статьи ещё не созданы;
- первым принят непубличный draft-кластер гостевого доступа;
- ссылки на draft не добавляются до редакционного подтверждения;
- оплата гостей требует claim ledger для платёжных и нормативных утверждений;
- отчётность владельца требует решения о коммерческом родителе.

Статус закрытого preview 2026-07-27 и production 2026-07-28:

- все три статьи подготовлены, прошли редакционный, маркетинговый,
  технический и SEO/GEO review и приняты владельцем;
- для статей подготовлены и приняты три отдельные обложки;
- статьи и обложки зафиксированы в коммите `fcf6263`;
- контекстные ссылки из `/demo`, профильных возможностей и существующих
  обзорных статей подготовлены и зафиксированы в коммите `fdb8606`;
- typecheck, lint и production build на Node.js 22 пройдены, сгенерированы
  `106/106` маршрутов;
- статьи, внутренняя перелинковка и новые обложки выпущены в production;
- новые обложки опубликованы в release `47529cc` и проверены на
  desktop/mobile;
- дальнейшая задача направления — накопить данные входов в статьи, переходов
  в demo/quiz и реальных обращений, не обещая SEO-результат по факту выпуска.

### LANDING-GROWTH-001 — упрощённые посадочные по боли клиента

Цель: сохранить основной сайт как подробный экспертный ресурс и добавить
альтернативный короткий вход для неподготовленного заказчика.

Статус: разработка и production release выполнены 2026-08-04.

- после сравнительного исследования сохранены две разные маркетинговые
  гипотезы: `/parkovka` и `/parkovka-pod-klyuch`;
- обе страницы встроены в общий Header/Footer, cookie consent, lead registry,
  GA4, Метрику и AI-консультант сайта;
- одинаковая форма и общий основной CTA позволяют сравнивать концепции, а не
  разные механики заявки;
- лендинги получили входы из главной, меню, футера, решений, оборудования и
  статей;
- служебная preview-плашка в production отсутствует;
- `/puzzle2` перенаправляется на `/parkovka-pod-klyuch`;
- старые варианты сохранены в Git, но недоступны публично;
- release `279d919820938e4ea87dcdd7a6138774df55f8c1` прошёл build, тесты,
  автоматический rollback двух некорректных контрольных запусков и финальную
  приёмку; итоговый процесс PM2 — `online`;
- индексация обеих рекламных страниц пока выключена осознанно: сначала нужен
  рекламный/поведенческий baseline и решение о поисковом интенте каждой
  страницы.

Следующий цикл `LANDING-GROWTH-002`:

1. накопить минимум 14 дней данных либо достаточный объём рекламного трафика;
2. сравнивать отдельно `view → CTA → form start → form success` и
   `AI open → first message → engaged chat → lead handoff`;
3. не выбирать победителя по просмотрам: основной показатель — рабочий лид,
   вспомогательные — начало формы, вовлечённый AI-диалог и качество лида;
4. проверить поисковые интенты и каннибализацию с существующими страницами;
5. только после этого решить: индексировать одну страницу, обе с разными
   интентами или оставить их рекламными `noindex`-лендингами;
6. новые посадочные создавать только под подтверждённую отдельную боль и с
   собственным измеримым коммерческим сценарием.

Подробный read-only аудит и границы реализации:
`docs/site/LANDING_GROWTH_001_20260728.md`.
Релизный акт:
`docs/deployment/LANDING_RELEASE_20260804.md`.

## 5. P1 — AI-виджет

Направление зафиксировано отдельно:

`docs/site/AI_WIDGET_ROADMAP_20260723.md`.

Действующий принцип:

- виджет — отдельный ограниченный публичный агент;
- не подключать его к широкому OpenClaw `main`;
- сначала утверждённая база знаний и FAQ;
- preview и production-профили разделены;
- публичный браузер обращается только к VPS API;
- VPS обращается к отдельному gateway Mac Studio через Tailscale HTTPS;
- реальный lead/handoff только после согласия и подтверждённого tool result;
- terminal, filesystem, browser, Codex, другие сессии и команды оборудованию
  запрещены;
- FAQ, маршрутизация и каталог ссылок обновляются на Mac Studio отдельным
  контролируемым gateway release без обязательного релиза сайта на VPS.

Статус `AI-WIDGET-0` на 2026-07-27:

- локальный документационный кандидат собран в `docs/site/ai-widget/`;
- актуальные страницы сайта получили приоритет над старой AI-базой;
- FAQ, claim ledger и data/lead policy подготовлены; продуктовые решения
  подтверждены владельцем;
- runtime, model gateway, widget UI, lead registry и MAX не подключались;
- подтверждены собственная разработка и производство, работа с 2010 года,
  более 350 реализованных объектов, обязательность имени и непубликация цен;
- подтверждены retention и закрытый пилот на Mac Studio;
- изолированный `cascade v3` adapter прошёл короткий контроль 15/15 без
  опасных ошибок, ложных handoff и внешних отправок;
- полный прогон завершён 230/230; ручной QA обнаружил один ценовой ориентир,
  после исправления ценовая регрессия прошла 17/17, boundary-регрессия 11/11;
- FAQ утверждён только для закрытого пилота;
- соседний рабочий AI-проект, OpenClaw, MAX, production registry и VPS не
  изменялись;
- следующий gate — закрытый UI/API-стенд на Mac Studio с CRM-симулятором.

Статус production-подготовки на 2026-07-28:

- владелец подтвердил подготовку полноценного публичного виджета для VPS;
- preview и production профили разделены;
- реальные лиды подключены к существующему реестру и MAX outbox;
- Сергей видит все лиды и диалоги, Андрей сохраняет право удаления;
- журнал диалогов, persistent rate limit, retention cleanup и fallback
  реализованы;
- дальнейшие изменения FAQ, промпта и маршрутизации выполняются на Mac Studio
  без повторного релиза VPS;
- закрытый HTTPS-канал VPS → Mac Studio работает через Tailscale;
- production опубликован на SHA
  `c65b1b3cee0eb946b2f28b7c59aeaf003681477c`;
- desktop/mobile и отсутствие виджета в `/admin` проверены;
- заявка `RSP-42254644` попала в lead registry, назначена Сергею и доставлена
  в MAX ровно один раз;
- транспортный timeout первого ответа закрыт значением Nginx `120s`, но
  ускорение uncached ответа остаётся ближайшей задачей;
- evidence:
  `docs/site/ai-widget/AI_WIDGET_PRODUCTION_ACCEPTANCE_20260728.md`.

Статус gateway hardening на 2026-07-28:

- production gateway запущен из release
  `80ffd254003eb45c0026db0598c83a0f6d5e830c`;
- добавлены prewarm responder context и быстрые детерминированные маршруты;
- уточнён сценарий разового клиента с собственным идентификатором:
  оплата на выезде или в совместимом кассовом терминале, без предложения
  онлайн-оплаты по неизвестному посетителю внутреннему идентификатору;
- добавлен allowlist-каталог контекстных ссылок на страницы сайта;
- текущая продуктовая работа — review реальных диалогов, FAQ/маршрутизация,
  качество и latency; это не требует автоматического изменения VPS-кода.

Статус объединённого VPS release на 2026-08-04:

- site-wide AI funnel analytics и контекст двух лендингов опубликованы;
- контекст `/parkovka` без предварительного выбора карточки теперь безопасно
  принимается VPS API;
- произвольные значения проблемы и контекст с другой страницы отклоняются;
- в репозиторий добавлены расширенная база ответов, маршрутизация и regression
  tests для качества диалога;
- site release `279d919820938e4ea87dcdd7a6138774df55f8c1` прошёл 46 gateway tests,
  13 cascade tests, 5 review tests, typecheck, lint и build 116 страниц;
- безопасный production smoke создал только тестовый диалог без лида и без
  сообщения в MAX.

Статус AI context hotfix на 2026-08-04:

- на обычных страницах виджет больше не формирует пустой `pageContext`;
- строгая проверка контекста рекламных лендингов сохранена;
- production release
  `5223dd80f6db384ca52685088522c9aba4c3f86a` завершён с автоматическими
  проверками и PM2 `online`;
- внешний browser smoke с главной страницы подтвердил полный путь
  `вопрос → подготовка → ответ со ссылками` без заявки и MAX;
- локальный release-test журнала больше не зависит от календарного истечения
  фиксированной тестовой даты.

Граница фактического выпуска:

- production gateway Mac Studio на момент сверки 2026-08-04 всё ещё запущен
  из release `80ffd254003eb45c0026db0598c83a0f6d5e830c`;
- поэтому изменения Python gateway и `WIDGET_RESPONSE_KNOWLEDGE_V1.md` из
  `279d919` нельзя считать активными только по факту VPS release;
- следующий отдельный блок `AI-GATEWAY-RELEASE-20260804` — собрать Mac Studio
  release, выполнить regression/eval, сохранить rollback, переключить
  LaunchAgent и проверить несколько реальных вопросов без создания лида;
- до этой приёмки site context считается выпущенным, а новая логика качества
  ответов — подготовленной, но не подтверждённой в runtime.

## 6. P2 — SEO/GEO и контентная система

### SEO-GEO-CYCLE-20260804 — следующий основной цикл

Цель: перейти от наращивания страниц к управляемому росту видимости,
цитируемости и заявок на основе реальных данных.

Порядок:

1. **Технический и индексный baseline.** Снять одинаковые срезы Google Search
   Console и Яндекс Вебмастера за 7/28/90 дней: индекс, исключения, sitemap,
   canonical, запросы, страницы входа, показы, клики и CTR.
2. **Проверка новых landing routes.** Решение об индексации принято:
   `/parkovka` и `/parkovka-pod-klyuch` опубликованы в sitemap; проверить их
   обнаружение после свежего обхода, редирект `/puzzle2` и отсутствие архивных
   вариантов в выдаче.
3. **Карта спроса и каннибализации.** Сопоставить запросы с существующими
   решениями, возможностями, оборудованием, кейсами и статьями; для каждого
   кластера выбрать одну основную страницу.
4. **GEO/entity layer.** Уточнить Organization/Service/Article/CaseStudy,
   автора и редакционного владельца, даты публикации/обновления, город, регион,
   тип объекта и оборудование кейсов. Schema должна подтверждаться видимым
   текстом.
5. **Answer-first улучшения.** Для страниц с показами и низким CTR либо
   слабой вовлечённостью доработать title/description, первый экран, короткий
   прямой ответ, FAQ и внутренние ссылки. Не создавать новые страницы до
   проверки существующей.
6. **Контентный пакет №1.** Выбрать максимум три материала из коммерческого
   спроса по данным кабинетов и отдела продаж; каждый проходит claim ledger,
   anti-duplication и отдельное подтверждение публикации.
7. **Измерение конверсии.** Собрать единую воронку
   `search/ad → landing/content → CTA/AI → form/lead → status`; не передавать
   PII, тексты диалогов и UTM-значения в аналитику.
8. **Решение через 30 дней.** Зафиксировать, какие страницы усиливать,
   объединять, оставлять рекламными или индексировать. Не оценивать SEO по
   одному запросу и не обещать позиции.

Первый артефакт цикла — обновлённый dashboard с числами и списком URL, а не
новый текстовый пакет. Начальная карта сохранена в
`docs/site/SEO_QUERY_PAGE_MAP_20260805.md`; её пересматривают после обработки
sitemap с 80 URL.

### CONTENT-SEO-GEO-PACK-001

На основании первого официального среза позиций и 54 утверждённых запросов
подготовлена карта `docs/site/SEO_CONTENT_PRIORITY_MAP_20260806.md` и локальный
пакет для трёх существующих страниц:

- `/` сохраняет широкий кластер `автоматизация парковки/парковок`;
- `/resheniya` получает системный кластер
  `автоматизированная парковочная система/системы`;
- `/oborudovanie` получает кластер оборудования для автоматизации, платной и
  закрытой парковки и распределяет товарный спрос на дочерние страницы.

Новые страницы не создавались. `/parkovka` остаётся рекламно-конверсионной,
а `/parkovka-pod-klyuch` не расширяется до накопления собственного поискового
среза. Следующий gate — техническая и визуальная приёмка локального кандидата,
после неё отдельное решение о публикации и измерение не менее двух циклов.

### GEO-CONTENT-001

Read-only audit recorded in `docs/site/GEO_CONTENT_AUDIT_20260805.md`.
The first implementation package is now implemented locally from owner-confirmed
source data recorded in `docs/site/GEO_CONTENT_SOURCE_INPUT_20260805.md`;
production publication remains a separate gate. No mass metadata edits outside
the approved priority package are authorized.
The first local candidate removes inherited/template and filesystem dates while
preserving source-backed ISO dates; it is verified but not published.
The source-status inventory for remaining entity fields is recorded in
`docs/site/GEO_ENTITY_SOURCE_LEDGER_20260805.md`.

- добавить Breadcrumb/FAQ JSON-LD на динамические страницы возможностей;
- добавить уместную Service-разметку на коммерческие страницы;
- добавить собственные metadata/canonical юридическим страницам;
- нормализовать для 30 кейсов `city`, `region`, `objectType`, `equipment`;
- добавить статьям `datePublished`, подтверждённого автора/редактора и источник;
- прекратить использовать filesystem mtime как редакционную дату sitemap;
- добавить подтверждённые `logo` и `sameAs` в Organization;
- проверять schema после каждого шаблонного изменения.

Owner-confirmed scope on 2026-08-06:

- all 12 article publication dates, author `Команда РОСПАРК`, publisher
  ООО «СР Эксперт» and `/logo.png` are approved;
- unavailable VK/RuTube profiles remain omitted from `sameAs`;
- 11 priority cases have approved public names, entity fields, equipment and
  result wording, including explicit corrections and restrictions for W-Plaza,
  «Мосфильм», «Горизонт» and «Элма-Курьяново»;
- barrier specifications and compatibility remain blocked pending engineer
  passports with test and OTK evidence.

Local candidate completed on 2026-08-06:

- all 12 articles now expose source-backed `datePublished`, author
  `Команда РОСПАРК` and legal publisher ООО «СР Эксперт»;
- Organization JSON-LD uses the approved `/logo.png` and still omits unavailable
  VK/RuTube profiles;
- 11 priority cases now carry normalized public object name, city, region,
  object type and equipment fields in CaseStudy JSON-LD;
- corrected visible facts for AMAKS, «Горизонт», W-Plaza,
  «Элма-Курьяново» and «Мосфильм»; W-Plaza remains explicitly an
  uncommissioned project with expected, not achieved, results;
- source-backed case dates remain omitted;
- focused GEO tests, TypeScript, ESLint and the 116-page Node.js 22 build pass.

### CONTENT-INTELLIGENCE-001

Существующая документация и шаблоны пока не являются исполняемым конвейером.

Безопасная последовательность:

1. заполнить allowlist источников;
2. провести два ручных запуска;
3. добавить дедупликацию и claim ledger;
4. хранить raw/черновики вне `content/stati`;
5. использовать статусы review;
6. создавать не более одного publish-ready draft за запуск;
7. публиковать только после отдельного подтверждения;
8. MAX draft и статья согласуются раздельно.

### PERFORMANCE-002

- проверить `hero.mp4` около 11 MB;
- определить бюджет видео и изображений;
- отделить активные web-ассеты от master-файлов;
- проверить cache headers Nginx;
- измерить LCP/CLS/INP на production;
- не удалять master-файлы без отдельного решения.

Локальный кандидат главного видео готов: 12,0 → 1,76 МБ, SSIM 0,98,
версионный URL и точечный immutable cache; оригинал сохранён, production не
изменён.

Дополнение накопительного релиза 2026-08-06: добавлен poster менее 150 КБ,
MP4 ограничен desktop без reduced motion, а изображения типов объектов
переведены на responsive Next Image. Технический SEO-контракт дополнительно
закрывает от обхода служебные API, админку, embed, quiz и закрытые demo,
не блокируя публичный `/demo`. Статус: `LOCAL_CANDIDATE_VERIFIED`.

### LANDING-MOBILE-001 — мобильная адаптация блоков на главной

Получена обратная связь клиента: два последних добавленных на главную страницу
блока со входами в рекламные лендинги не адаптированы для мобильных экранов.

Задача следующего UI-пакета:

- определить оба фактических блока по DOM и визуальному порядку на production;
- исправить переполнение, переносы, размеры изображений, CTA и вертикальные
  интервалы без изменения согласованных смыслов;
- проверить ширины 320, 375 и 390 px, а также desktop 1440 px;
- проверить отсутствие горизонтальной прокрутки и перекрытия AI-виджетом;
- выполнить локальную визуальную приёмку и отдельный production smoke после
  включения в накопительный релиз.

Статус: `LOCAL_CANDIDATE_VERIFIED`. Две карточки `LandingChoiceSection`
получили мобильные отступы, переносы, `min-width: 0`, компактную типографику и
равномерное расположение CTA без изменения согласованных смыслов. Проверены
320/375/390/1440 px: горизонтального переполнения нет, ширина карточек не
превышает контейнер. Production smoke остаётся отдельным release gate.

## 7. P2 — публичный MAX

Разделять:

1. внутренний MAX-чат заявок;
2. demo-отправку гостю;
3. публичный канал РОСПАРК;
4. будущий диалоговый бот/мини-приложение.

Наличие рабочей demo-отправки не доказывает создание публичного канала.

До publisher:

- подтвердить официальный URL и владельца канала;
- провести ручной период 4–6 недель;
- использовать UTM;
- хранить токен вне Git;
- allowlist одного channel ID;
- dry-run и защита от дублей;
- одноразовое подтверждение каждого поста.

## 8. Последний этап — SECURITY-RELEASE-2

По решению владельца от 2026-07-23 обновление зависимостей переносится на
последний этап roadmap. Причина приоритизации — сейчас важнее завершить
продуктовую упаковку, измерение SEO/GEO, аналитику, обработку лидов и пилотные
сценарии.

Это решение означает принятие временного технического риска, а не отсутствие
риска. Публичная доступность сайта сама по себе создаёт поверхность атаки,
независимо от активности конкурентов и текущего объёма посетителей.

Этап нужно вернуть раньше в план при любом из условий:

- начинается активное продвижение или заметно растёт публичный трафик;
- расширяются формы, API, AI-виджет или внешние интеграции;
- появляется security-инцидент или новая critical-уязвимость;
- меняется production runtime;
- планируется крупный production-релиз.

Зафиксированный объём этапа:

1. создать отдельную upgrade-ветку;
2. выбрать поддерживаемую исправленную версию Next.js после проверки
   совместимости, а не обновлять production напрямую;
3. обновить Nodemailer, Sharp и транзитивные зависимости;
4. повторить typecheck, lint, production build и dependency audit;
5. проверить все 100 build-маршрутов;
6. повторить Stage B и Stage C demo-smoke;
7. провести desktop/mobile/a11y-приёмку трёх demo-сценариев;
8. провести повторный security audit;
9. выпускать только отдельным Release 2 с rollback-планом.

Исходная фиксация на 2026-07-23: `npm audit --omit=dev` показывал пять
production-зависимостей уровня high, включая прямые `next`, `nodemailer` и
`sharp`, а также транзитивные `postcss` и `js-yaml`.

Критерий готовности: нет неисследованных high/critical production findings,
все функциональные и визуальные проверки пройдены на Node.js 22.

## 9. Порядок следующих блоков

1. `DOCS-RECONCILE-20260804` — зафиксированы два лендинга, AI-аналитика,
   фактический Mac gateway `80ffd25`, production `279d919` и последующий
   context hotfix `5223dd8`; источник правды переключён на следующий
   SEO/GEO-цикл.
2. `AI-GATEWAY-RELEASE-20260804` — отдельно активировать и принять на Mac
   Studio подготовленную логику качества ответов из `279d919`; VPS повторно не
   деплоить без необходимости. Кандидат exact SHA собран, 46+13+5 тестов и
   живая проверка 13 вопросов пройдены; переключение LaunchAgent ждёт
   отдельного production-разрешения.
3. `SEO-OPS-001-B / SEO-GEO-CYCLE-20260804` — первый фактический срез выполнен
   2026-08-04: Search Console показывает 11 кликов, 250 показов и 57
   проиндексированных URL; Яндекс подтвердил первые запросы и один дубль
   description на двух страницах шлагбаумов. Точные списки 5 URL `404` и 23 URL
   «Обнаружена, не проиндексирована» выгружены и классифицированы. Уникальные
   description, canonical redirect `/index.html`, `SEO-INDEXING-002` и
   индексируемый режим лендингов опубликованы. Sitemap повторно отправлен в оба
   кабинета; следующий шаг — дождаться свежего обхода и сравнить новый срез с
   baseline.
4. `ANALYTICS-001-E / LANDING-GROWTH-002` — native envelope команд `gtag.js`
   опубликован в `2fc363e`; `rospark_ai_lead_handoff` отмечен key event. После
   окна обработки повторить read-only срез и подтвердить первый обезличенный
   `page_view`. Для Метрики получить доступ владельца `radicom`, затем начать
   сопоставимое измерение двух лендингов, форм и AI-воронки.
5. `SEO-PERF-GEO-001` — опубликован в `c57887f`: WebP тяжёлых изображений
   `/parkovka`, Service JSON-LD двух утверждённых лендингов и FAQ JSON-LD
   страницы «Парковка под ключ»; публичные Lighthouse и JSON-LD проверены.
6. `SEO-PERF-GEO-002` — опубликован в `2fc363e`: responsive Next Image для
   `/parkovka-pod-klyuch`, native GA4 envelope и точечный 308
   `/mobile/index.html` с сохранением query string; HTTPS, AI status, БД и
   no-send gate пройдены.
7. `LANDING-MOBILE-001` — локальный кандидат принят на 320/375/390/1440 px;
   включить в накопительный релиз и выполнить отдельный production smoke.
8. `GEO-CONTENT-001` — первый source-backed пакет локально готов; включить в
   накопительный релиз, затем проверить публичный JSON-LD и видимый текст без
   генерации лидов.
9. `CONTENT-SEO-GEO-PACK-001` — выбраны `/`, `/resheniya` и `/oborudovanie`;
   карта спроса и локальный кандидат готовы, публикация отдельно.
10. `LEAD-OPS-002 / DEMO-GROWTH-001 / CONTENT-DEMO-001` — завершены как
   разработка; вести эксплуатационный мониторинг очереди, SLA, retention,
   demo/quiz и опубликованных статей.
11. `CRM-INTEGRATION-001` — discovery и последующая интеграция заявок с
   действующей amoCRM; до настройки сайт сохраняет собственный реестр.
12. `PERFORMANCE-002` — измерить production CWV, затем оптимизировать самые
   тяжёлые hero/video/assets и cache headers. Локальное дополнение Hero poster,
   mobile/reduced-motion media gate, responsive object images и robots guard
   готово к накопительному релизу; production CWV измеряется после публикации.
13. `CONTENT-INTELLIGENCE-001` — ручной, затем локальный approval-gated runner.
14. `PROD-DATA-OPS` — отложен; вернуть перед крупным production-изменением.
15. `SECURITY-RELEASE-2` — перенесённый этап зависимостей; вернуть раньше при
   активном росте трафика или новом high/critical finding.

Режим накопительного релиза, подтверждённый 2026-07-27:

- принятые блоки сохраняются отдельными локальными коммитами;
- закрытый preview обновляется по мере необходимости для приёмки;
- VPS не обновляется после каждой небольшой правки;
- production-пакет собирается из нескольких принятых совместимых блоков;
- выключенные R&D-компоненты не становятся публичными автоматически;
- перед единым выпуском выполняются общий diff review, backup, build/smoke и
  один rollback-план.

## 10. Решения директора

Подтверждено 2026-07-24 для `LEAD-OPS-002`:

- источник истины — отдельный реестр на VPS; CRM пока без настроенной
  интеграции;
- владелец данных и административного контроля — директор;
- операционный ответственный за все новые лиды по умолчанию — Сергей, РОП;
- рабочие часы — понедельник–пятница, `10:00–18:00`, `Europe/Moscow`;
- SLA первого контакта — один рабочий час;
- основной канал — MAX-группа отдела продаж;
- Email — будущий резервный канал, пока не настроен;
- обычные лиды — 60 дней, demo feedback — 30 дней;
- дубль — тот же телефон в течение 24 часов от последней заявки при незакрытом
  лиде;
- результаты закрытия — обработан, нет связи, нецелевой, дубль, тест.
- Андрей — полный административный доступ, export и ручное удаление;
- Сергей, РОП — просмотр, обработка и export без удаления и без VPS/SSH.
- после подключения amoCRM распределение между будущими менеджерами выполняет
  Сергей внутри amoCRM; временный кабинет не расширяется до отдельной CRM.

Остаётся определить:

1. владельца Search Console, Яндекс Вебмастера и аналитики;
2. резервного администратора Метрики и правила доступа;
3. владельца публичного MAX-канала;
4. доступ и параметры API действующей amoCRM для `CRM-INTEGRATION-001`;
5. решение об индексации `/parkovka` и `/parkovka-pod-klyuch` после
   измерительного цикла `LANDING-GROWTH-002`;

Подтверждено директором 2026-07-27 для `AI-WIDGET-0`:

- Андрей — владелец пилота и данных, Сергей — рабочий пользователь будущих
  лидов;
- собственная разработка и производство оборудования;
- работа с парковками с 2010 года;
- более 350 реализованных объектов;
- имя обязательно для реальной карточки обращения;
- цены отдельных элементов не публикуются;
- обычный lead retention остаётся 60 дней;
- анонимная активная сессия — 24 часа;
- синтетический test transcript — максимум 7 дней;
- operational logs без PII и текста — максимум 14 дней;
- закрытый пилот размещается на Mac Studio;
- постоянная публичная схема размещения решается позднее.

## 11. Рабочий протокол

- один логический блок — один понятный diff;
- UI-блок — localhost desktop/mobile и подтверждение;
- код — typecheck, lint, build, `git diff --check` и целевой smoke;
- нативные модули — только под Node.js 22;
- контент — `draft → review → preview → approval → publication`;
- публичные факты — только из claim ledger;
- production, секреты, реальные сообщения и персональные данные — только по
  отдельному подтверждению;
- успешный локальный тест не равен production-ready;
- техническая готовность к индексации не равна гарантированным позициям.
