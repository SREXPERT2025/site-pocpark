# РОСПАРК - актуальный план улучшения сайта

Дата актуализации: 2026-07-09
Текущая рабочая ветка: `feature/site-20260706-homepage-hygiene`
Последняя сверенная точка: `6f7f572 fix(images): serve optimized webp assets`
Статус: большая часть плана от 2026-07-06/2026-07-07 уже закрыта. Документ ниже является текущим источником правды для следующего цикла работ.
Дополнение от 2026-07-09: после фидбека РОП Сергея добавлена стратегия лаконичности и читаемости: `docs/site/SERGEY_ROP_READABILITY_STRATEGY_20260709.md`.

## 1. Что уже закрыто

### 1.1. Базовая SEO/GEO и техническая основа

- Канонический production URL и canonical metadata внедрены.
- Sitemap и metadata для ключевых страниц усилены.
- Organization JSON-LD подключен глобально.
- Каталог и карточки оборудования получили Product JSON-LD, FAQ/Breadcrumb schema и SEO-метаданные.
- CaseStudy JSON-LD добавлен для кейсов.
- `/api/quiz` безопасно закрыт как legacy endpoint, активные формы отправляют заявки через `/api/lead`.
- Контакты, публичный email и NAP-данные приведены к понятной роли.
- Подключен lint-скрипт `next lint --max-warnings=0`.

Ключевые коммиты: `95d1a73`, `075cb85`, `0bc933e`.

### 1.2. Заявки, контекст лида и квиз

- API принимает и `message`, и `comment`, поэтому текст из обычных форм не теряется.
- В заявке сохраняются `source`, `intent`, `scenario`, `product`, `sourceUrl` и UTM/клик-идентификаторы.
- Delivery policy усилена: успешной считается только реально доставленная заявка хотя бы в один канал.
- Telegram delivery проверяет `res.ok`; ошибки доставки логируются.
- Квиз стал проектным брифом с вопросами про тип объекта, город, въезды/выезды, стадию, цель обращения и текущую систему.
- Проектный бриф в квизе сделан мягким: пользователь может оставить заявку без ощущения длинной анкеты.
- Добавлены контекстные scenario tags для заявок.

Ключевые коммиты: `0bc933e`, `ddae86b`, `6f6bf1e`, `fbb2f21`.

### 1.3. Навигация, CTA и конверсионный маршрут

- CTA из markdown нормализованы, поддержаны разные форматы фронтматтера.
- Битые download-ссылки оборудования убраны или нормализованы.
- В меню и footer раскрыты важные страницы процесса и решений.
- В меню добавлены ключевые solution hubs и технологические маршруты.
- Keyboard-доступность меню улучшена.
- Коммерческие CTA приведены к сценариям квиза.

Ключевые коммиты: `add9838`, `f9f842f`, `b57fa82`, `90ac625`.

### 1.4. Хабы, страницы и контент

- Создана полезная 404 recovery page.
- Создан полноценный хаб `/resheniya`.
- Усилен хаб `/vozmozhnosti`.
- Добавлены и усилены страницы:
  - `/resheniya/stoimost-avtomatizacii-parkovki`;
  - `/resheniya/integracii-i-api`;
  - `/resheniya/skladskie-kompleksy`;
  - `/oborudovanie/shlagbaumy`.
- Усилены страницы онлайн-оплаты и распознавания номеров.
- Добавлены статьи и визуальные cover images для articles hub.
- Обновлены тексты и визуалы по части статей.
- Названия типов объектов раскрыты без внутренних аббревиатур.
- На главной обновлен стаж компании до 16 лет.

Ключевые коммиты: `ed4315b`, `cdfaa2f`, `b44e489`, `251250e`, `0fe409b`, `6c9158c`, `b4cdd84`, `c7a7b44`, `9236145`, `cf66554`, `51d9e65`.

### 1.5. Визуалы и performance

- Визуальные блоки переведены на `next/image`, где это было нужно.
- Добавлены explainer visuals для решений и возможностей.
- Тяжелые PNG/JPG в активных местах сайта заменены ссылками на заранее подготовленные `.webp`.
- Пример: cover статьи `onlain-oplata-parkovki-kak-vnedrit` уменьшен примерно с 2 MB до 162 KB.
- Проверка `npm run build` после оптимизации изображений прошла успешно.

Ключевые коммиты: `40a7d87`, `4f644b9`, `1502075`, `33a06eb`, `43782b8`, `6f7f572`.

## 2. Что осталось закрыть в ближайшем цикле

### P0.1. Production/VPS smoke после обновления

Цель: убедиться, что ветка `feature/site-20260706-homepage-hygiene` корректно работает на VPS после pull/build/restart.

Проверить:

- главная страница;
- статья с тяжелым cover, например `/stati/onlain-oplata-parkovki-kak-vnedrit`;
- `/quiz`;
- `/contacts`;
- `/resheniya`;
- `/vozmozhnosti`;
- карточка оборудования и один кейс;
- что `.webp` реально отдаются с VPS;
- что заявка доходит в согласованный канал.

Проверки:

- `npm run build` на VPS;
- ручной smoke в браузере;
- тестовая заявка с пометкой `SMOKE TEST`, если владелец подтверждает отправку.

### P0.2. Убрать tracked-файлы-дубликаты `— копия`

В `app/` все еще отслеживаются локальные копии компонентов:

- `app/(narrow)/oborudovanie/[slug]/ProductView.tsx — копия`
- `app/(narrow)/resheniya/dlya-rukovoditeley/components/Solution.tsx — копия`
- `app/(narrow)/resheniya/dlya-sluzhby-bezopasnosti/components/Solution.tsx — копия`
- `app/components/FeaturesShowcase.tsx — копия`
- `app/components/FeaturesShowcase.tsx — копия 2`

Действие:

- сравнить с актуальными файлами;
- убедиться, что они не импортируются;
- удалить отдельным маленьким коммитом.

Проверки:

- `rg` по импортам;
- `npm run build`;
- `git diff --check`.

### P0.3. Smoke форм и privacy-safe analytics

Цель: проверить не только код, но и реальный пользовательский путь.

Проверить:

- `/contacts` -> `/api/lead`;
- `/quiz` -> `/api/lead`;
- контекст лида в тексте заявки;
- отсутствие PII в локальных analytics events;
- понятное сообщение об ошибке при проблеме доставки.

Ограничение: тестовую отправку делать только после подтверждения владельца, чтобы не шуметь в рабочих каналах.

### P0.4. Принять новый стандарт лаконичности страниц

Источник: фидбек РОП Сергея и субагентский аудит от 2026-07-09.

Главный принцип:

- один экран - одна мысль;
- крупные смысловые блоки вместо множества мелких карточек;
- минимум мелкого текста и пестрой инфографики;
- подробности не удалять, а уводить в `details`, FAQ, статьи, extended info и schema-совместимые слои.

Опорный документ:

- `docs/site/SERGEY_ROP_READABILITY_STRATEGY_20260709.md`

Первый практический шаг:

- добавить базовые компоненты нового стандарта;
- перевести `/resheniya` на новый лаконичный layout;
- проверить desktop/mobile screenshots;
- после согласования раскатать паттерн на `/vozmozhnosti`.

## 3. Следующие продуктовые улучшения

### P1.1. Пилот нового лаконичного стандарта на `/resheniya`

Цель: сделать эталонную страницу по фидбеку РОП, не переписывая весь сайт одним большим изменением.

Что сделать:

- заменить четыре однотипные сетки на 2-3 крупных входа:
  - по типу объекта;
  - по роли;
  - по этапу проекта;
- сократить descriptions в карточках;
- убрать лишние label chips и мелкие подписи;
- сохранить ItemList JSON-LD;
- обкатать `PageHero`, `PageSection`, `SectionHeader`, `SimpleCard`, `CTASection`.

Проверки:

- `npm run build`;
- desktop/mobile screenshots до/после;
- визуальная сверка по чек-листу из стратегии.

### P1.2. Главная страница: финальный hygiene pass

Часть работы уже сделана, но нужен финальный проход как по коммерческой витрине.

Проверить и улучшить:

- первый экран: ясность оффера, CTA, мобильная высота;
- последовательность блоков: возможности -> типы объектов -> роли -> коммерческий блок -> форма;
- повторяемость CTA и внутренних ссылок;
- отсутствие лишних обещаний, неподтвержденных сроков и декоративного шума;
- поведение hero video на мобильных и медленных соединениях.

Ограничение: не смешивать с API/формами и глобальной навигацией.

### P1.3. Внутренняя перелинковка solution -> equipment -> cases -> quiz

Добавить аккуратные контекстные переходы:

- страницы решений ведут к релевантному оборудованию;
- оборудование ведет к подходящим кейсам и квизу;
- кейсы ведут к похожим решениям и заявке;
- статьи ведут к связанным решениям, возможностям и оборудованию.

Цель: усилить SEO/GEO, AI-search и путь пользователя без расширения верхнего меню.

### P1.4. `/vozmozhnosti`: лаконичный хаб вместо перегруженной витрины

Цель: убрать ощущение каталога и пестрой инфографики.

Что сделать:

- заменить текущую подачу на три крупных контура:
  - доступ;
  - оплата;
  - контроль;
- оставить 6 возможностей как крупные навигационные блоки;
- убрать или радикально упростить `FeaturesShowcase`;
- подробности увести на дочерние страницы;
- сохранить ItemList/Breadcrumb JSON-LD.

### P1.5. Страницы `/vozmozhnosti/*`: второй SEO/GEO проход

Для дочерних страниц возможностей:

- добавить или проверить answer-first блок;
- добавить FAQ и FAQPage schema там, где есть реальные вопросы;
- добавить Breadcrumb schema, если ее нет;
- добавить ссылки на решения, оборудование, статьи и кейсы;
- не выдумывать характеристики, цены, гарантии или неподтвержденные сценарии.

### P1.6. Ролевые страницы: укрупнить и сократить

Страницы:

- `/resheniya/dlya-rukovoditeley`;
- `/resheniya/dlya-inzhenerov`;
- `/resheniya/dlya-sluzhby-bezopasnosti`.

Цель: оставить 4 главных блока вместо длинной цепочки доказательств:

- Hero;
- что меняется для роли;
- как РОСПАРК решает;
- CTA.

Подробности оставить в `ExtendedInfo`/FAQ/details.

### P1.7. Кейсы: нормализовать frontmatter пакетами

Для 30 кейсов постепенно добавить:

- `city`;
- `region`;
- `objectType`;
- `equipment`;
- `result facts`;
- `related solution links`;
- `related feature links`.

Цель: улучшить фильтрацию, schema-разметку, внутреннюю сетку и пригодность кейсов для AI-search.

### P1.8. Performance pass 2: видео, master assets и кеширование

Изображения в активных местах уже облегчены, но остались системные решения:

- проверить `hero.mp4` около 11 MB;
- решить, храним ли тяжелые master PNG/JPG в `public` или переносим в отдельное хранилище/архив;
- настроить cache headers на VPS/Caddy для статических ассетов;
- проверить поведение первого захода после очистки `.next/cache/images`;
- зафиксировать целевые лимиты веса для новых визуалов.

## 4. Задачи, требующие подтверждения владельца

- Production deploy, VPS, DNS, SSL, Caddy/nginx, GitHub Actions.
- Тестовая отправка заявок в рабочие каналы.
- Подключение Yandex.Metrica / GA4 / GTM и webmaster verification.
- Финальное решение по основному коммерческому email, если нужно отличать `is@srexpert.su` и будущий sales-адрес.
- Любые цены, сроки внедрения, гарантии, сертификаты, отзывы, рейтинги и новые публичные обещания.
- Удаление тяжелых оригиналов изображений из `public`, если они нужны как master-файлы.

## 5. Рекомендуемый порядок выполнения

1. P0.1: smoke VPS после обновления ветки.
2. P0.3: smoke форм и доставки заявок.
3. P0.2: удалить tracked `— копия` файлы отдельным коммитом.
4. P0.4: принять новый стандарт лаконичности страниц.
5. P1.1: пилот нового стандарта на `/resheniya`.
6. P1.4: перестроить `/vozmozhnosti` по новому стандарту.
7. P1.2: финальный homepage hygiene pass.
8. P1.6: укрупнить и сократить ролевые страницы.
9. P1.3: усилить внутреннюю перелинковку.
10. P1.5: пройти дочерние страницы возможностей.
11. P1.7: нормализовать кейсы пакетами по 5-10 страниц.
12. P1.8: второй performance pass по видео, master assets и кешированию.

## 6. Рабочий протокол

- Каждая задача - отдельный аккуратный diff в текущей feature-ветке или отдельная ветка, если scope большой.
- Не трогать `main` напрямую.
- Не делать production deploy без отдельного явного подтверждения.
- Перед правкой фиксировать scope и файлы.
- После правки запускать минимум `npm run build`; для TypeScript/API/form changes также `npm run typecheck`.
- Для UI/visual changes проверять desktop и mobile.
- Для форм и delivery делать ручной smoke только после согласования тестовой заявки.
